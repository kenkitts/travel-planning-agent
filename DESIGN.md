# Travel Planning Agent — Design Decisions

Status: Approved (interview complete, 2026-08-17)

## 1. Purpose & Scope

An AI agent that conversationally builds day-by-day travel itineraries. It is
**not** a booking assistant — no flights/hotels/payments in scope. Booking
integrations are an explicit future extension, not part of this build.

## 2. Core Design Decisions

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 1 | Scope | Itinerary builder only (no booking) | Booking requires payment/PCI-adjacent integrations — large surface area for v1. Itinerary generation is demoable and extensible later. |
| 2 | Input style | Conversational gathering | Agent starts from a vague request ("plan me a trip to Japan") and asks follow-ups (dates, budget, pace, interests, travelers) before generating anything. Plays to LLM strengths vs. a rigid form. |
| 3 | Grounding tools | Web search + maps/places + weather | Pure-LLM itineraries hallucinate hours/prices and produce geographically illogical day plans. All three groundings are in scope for v1. |
| 4 | Tool providers | AWS-native: AgentCore Web Search (managed connector), Amazon Location Service (maps/places), Open-Meteo via Lambda (weather) | Minimizes external vendor keys; Web Search is a genuine GA AgentCore Gateway connector (verified via AWS docs — zero data egress, MCP-based). Location Service integrates via IAM, no separate key. |
| 5 | Tool access architecture | Single AgentCore Gateway with 3 targets: Web Search connector, Lambda target (weather), Lambda target (Location Service wrapper) | One consistent MCP interface, centralized IAM auth, easy to add more targets later (e.g. booking API). |
| 6 | Agent framework | Strands Agents SDK | AWS's own framework, built for AgentCore, first-class MCP/Gateway support, most current AgentCore samples use it. |
| 7 | Model | Claude Sonnet (latest via Bedrock) | Multi-day itinerary planning needs strong multi-step reasoning + reliable tool-use chaining across 3 tools. |
| 8 | Memory | AgentCore Memory — short-term AND long-term | Short-term: tracks in-session requirement gathering (dates, budget, etc.). Long-term: remembers user preferences (e.g. "prefers food-focused, budget travel") across sessions/trips. |
| 9 | Output format | Plain conversational markdown text (v1) | No structured itinerary schema exists yet; deferred until there's a real consumer that needs it, to avoid premature schema design. (Streaming/diagnostic surfacing of the same markdown output was added later — see decision #19 — but the output format itself is unchanged.) |
| 10 | Testing/invocation clients | AgentCore console test chat, local Python CLI REPL, and a local web UI | Console for zero-setup manual smoke tests; CLI for scripted/headless testing; local web UI (added later, see decision #19) for a more usable manual-testing/demo experience with live streaming and a diagnostics view. |
| 11 | IaC | AWS CDK (Python) | AgentCore has CDK L2 constructs (`aws_cdk.aws_bedrockagentcore`: `Memory`, `Gateway`, `GatewayTarget.ForLambda`, `CfnRuntime` — verified in CDK docs). Reproducible, version-controlled. |
| 12 | Application language | Python (agent code, Lambda handlers, CDK) | Strands SDK is Python-native; single-language stack avoids unnecessary context switching for two simple Lambda handlers. |
| 13 | Project type | Personal AWS account project, standard OSS tooling | Not a Brazil/internal-Amazon build. Plain `cdk deploy`, plain git repo. |
| 14 | Location & region | New local repo, `us-east-1` | Verified via `aws___get_regional_availability`: AgentCore, Location Service both available in `us-east-1` and `us-west-2`. us-east-1 chosen as default/most common launch region. |
| 15 | Auth for invoking agent | IAM auth only | Matches CLI/console testing tools, and later the local web UI (decision #19), all of which use IAM/SigV4-signed calls with local AWS credentials — no separate identity system. Open/no-auth explicitly rejected — AgentCore invocations cost real money and shouldn't be internet-exposed. |
| 16 | Weather provider | Open-Meteo | Free, no API key, global coverage (rules out US-only NWS), avoids Secrets Manager setup needed for OpenWeatherMap's key. |
| 17 | Cost posture | No hard budget constraint | Optimize for correct architecture over cost minimization; still defaulted to free-tier services where reasonable (Open-Meteo). |
| 18 | Testing strategy | Unit tests only (Lambda handlers + deterministic logic) | Itinerary output is LLM-generated/non-deterministic — integration tests would only verify "didn't crash," not quality. Manual testing (via #10) covers end-to-end behavior. |
| 19 | Local web UI | Single-user, local-only FastAPI app (`web/server.py`) calling the same deployed Runtime as the CLI, via a shared `cli/agent_client.py` | A more usable manual-testing/demo surface than the CLI or console, without standing up any new AWS infrastructure. Explicitly local-only (binds `127.0.0.1`) since it holds real AWS credentials and has no auth layer of its own — not a general-purpose hosted frontend (see decision #15, unchanged: IAM auth only, no separate identity system). |
| 20 | Response delivery | Live SSE streaming from `agent/agent.py`'s `invoke()` (an async generator `BedrockAgentCoreApp` auto-wraps as `text/event-stream`), consumed incrementally by both clients | Waiting for a full multi-tool-call turn to complete before showing anything is a poor interactive experience for multi-day itineraries that can involve a dozen-plus tool calls. The CLI consumes the same stream but only prints the final text, preserving its original non-streaming UX; the web UI renders text deltas live. |
| 21 | Diagnostics | Optional, off-by-default diagnostic panel in the web UI showing every labeled event (reasoning, tool_use, tool_result, error) from the same stream | Useful for understanding/debugging agent behavior (which tools were called, with what arguments, extended-thinking text) without adding permanent UI clutter or a separate observability stack. Reasoning deltas are accumulated into one growing entry (not one per token) and tool_result JSON is pretty-printed, since both arrive as raw fragments/unformatted text from the model. |
| 22 | Extended thinking | Enabled via Bedrock Converse `additional_request_fields: {"thinking": {"type": "adaptive", "display": "summarized"}}` | `display: "summarized"` is required to get any non-empty reasoning text back from Claude Sonnet 5 in this configuration — without it, reasoning content is present in the API but empty when rendered. Surfaced only in the diagnostic panel (see #21), not the main chat bubble, since it's not meant for end users. |
| 23 | Conversation history browsing | Web UI sidebar reads directly from AgentCore Memory (`ListSessions`/`ListEvents`), no separate local storage | Memory is already the durable source of truth for conversation history; a second local store would just be a cache to keep in sync. Session titles are stored as a dedicated marker event (via `CreateEvent`) since Memory has no native session-title field; deletion removes every event in a session one at a time (no session-level delete API). |
| 24 | Context growth control | `SummarizingConversationManager` (`proactive_compression=True, pin_first=6`) on the Strands `Agent` | Long-running conversations (many turns, many tool calls) grow context indefinitely under Strands' own default; proactive summarization bounds this instead of only reacting after hitting a context-window error. |
| 25 | Long-response handling | `max_tokens=8192` explicit on `BedrockModel`, with `MaxTokensReachedException` handled gracefully in `invoke()` | Bedrock Converse's default per-model max output token limit is too low for some multi-day, tool-grounded itineraries — confirmed in production, a 13-tool-call turn hit the limit mid-answer and surfaced as an opaque `InvokeAgentRuntime` 500 error. 8192 gives realistic headroom; the exception handler covers cases that still exceed it. |

## 2a. JWT Authorization (added 2026-08-26, supersedes decision #15)

Status: Approved (interview complete, 2026-08-26)

Decision #15 (IAM auth only) and decision #5's explicit exclusion of
"Cognito/JWT auth for non-AWS-credentialed clients" are superseded by this
section. The trigger: a small group of known users (beyond just the
original author) needs to use the CLI/web UI, and IAM/SigV4 with local AWS
credentials doesn't scale to "share this with a few people" without also
sharing AWS credentials — undesirable.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 26 | Auth cutover scope | Full replacement: AgentCore Runtime's authorizer switches from `RuntimeAuthorizerConfiguration.using_iam()` to `.using_jwt(...)` (custom JWT authorizer). No dual IAM+JWT path. | AgentCore Runtime supports one authorizer configuration at a time. Once JWT is configured, IAM/SigV4 calls to `InvokeAgentRuntime` no longer work for anyone — both clients (CLI and web UI) must move to bearer tokens together. |
| 27 | Token issuer | Okta (existing org) — a **new, dedicated Okta application** registered specifically for the Travel Agent, distinct from the author's pre-existing `okta-claude-code-token-helper` app (used for an unrelated Claude Code LLM gateway). | Reusing the same Okta app/client ID would let a token minted for one system silently authenticate to the other — undesired coupling. A separate app keeps blast radius and scope (audience, allowed scopes) contained to this project. |
| 28 | Users | Small, known group of individuals, each with their own Okta identity — not a single personal token, not open self-service signup. | Matches the actual near-term need (share access with a few specific people) without taking on a public-facing identity system. |
| 29 | Clients migrated | Both `cli/chat.py` and `web/server.py` move from IAM/SigV4 (via `boto3`'s default credential chain) to JWT bearer tokens on the `InvokeAgentRuntime` call. Neither client keeps an IAM fallback. | Consistent with decision #26 — IAM stops working for any caller once the Runtime's authorizer is switched, so there is no such thing as "just migrate one client." |
| 30 | Web UI hosting scope | Unchanged from decision #19: still local-only, single machine per user, binds `127.0.0.1`, no new hosting infrastructure. Each of the small group of users runs their own local copy and logs in with their own Okta identity against the one shared deployed Runtime. | Hosting the web UI somewhere network-reachable (TLS, a domain, cookie/session handling, public exposure) is a materially different and larger project than "add JWT auth" — explicitly deferred as a separate future effort, not bundled into this change. |
| 31 | Memory scoping / `actor_id` derivation | `actor_id` for AgentCore Memory is derived **server-side**, inside `agent/agent.py`, from the verified JWT's `sub` claim (Okta's stable, immutable per-user identifier) — not from a client-supplied `--actor-id` flag or any other client-controlled value. No override/impersonation escape hatch. | The whole point of adding real per-user auth is that one user's long-term Memory (preferences, conversation history) can't be read or written under another user's identity just by passing a different flag. `sub` (vs. `email`) is used because it's guaranteed stable even if a user's Okta profile email changes later — using `email` as the key would orphan old Memory records on any such change. An override flag "for testing" was explicitly rejected as a foot-gun that reintroduces the same spoofing risk it's meant to close; testing as a different persona means logging into Okta as that user, not passing a flag. |
| 32 | Token acquisition mechanism | Both clients acquire a bearer token by invoking the author's existing `~/okta-claude-code-token-helper/okta-claude-code-token.py` script as a **subprocess, once per request/turn** (not cached independently by the Travel Agent clients), reusing that script's own caching/silent-refresh/interactive-login/cross-process-locking behavior. Configuration (`OKTA_ISSUER`, `OKTA_CLIENT_ID`, `OKTA_SCOPES`, `OKTA_REDIRECT_PORT`) for the Travel Agent's dedicated Okta app (decision #27) lives in a new `travel-planning-agent/.env` (gitignored; `.env.template` committed) and is passed to the subprocess as real environment variables — not via that script's own `.env` file, which is reserved for the pre-existing Claude Code gateway app and would otherwise force a shared/renamed dotenv file the script doesn't natively support. | Reimplementing OAuth 2.0 Authorization Code + PKCE, token caching, silent refresh, and cross-process locking from scratch for this project would duplicate a script that already does exactly this and is already trusted for a different real use case. Re-invoking per turn (rather than the client caching a token itself) means a multi-hour session keeps working transparently across token expiry without any client-side refresh logic — the helper script already handles "valid → instant," "expired but refreshable → silent refresh," and "neither → interactive login." |
| 33 | Token cache collision | Accepted: the helper script's token cache path (`~/.cached-credentials/token-cache.json`) is a hardcoded constant, shared regardless of which Okta app's config is passed in via environment variables. Logging into the Travel Agent's Okta app evicts the Claude Code gateway's cached token and vice versa — only one is "logged in" at a time; switching tools forces a fresh interactive login for whichever wasn't used most recently. | Making the cache path configurable would require modifying the shared helper script (adding e.g. an `OKTA_CACHE_PATH` override) — explicitly deferred; the author chose to accept the collision for now rather than take on that (small, but nonzero) maintenance change. |
| 34 | Claim propagation mechanism | The Runtime is configured with `requestHeaderAllowlist: ["Authorization"]` alongside the `customJwtAuthorizer`. This does **not** happen automatically from JWT authorizer configuration alone — confirmed against AWS's own docs (`inbound-jwt-authorizer.html`, `runtime-header-allowlist.html`): the authorizer validates the token's signature/expiry/issuer/audience/client/scopes at the edge before the agent runs, but claims are not injected into the invocation context as a separate, pre-parsed field. The allowlisted raw `Authorization` header is forwarded to `agent/agent.py` via `context.request_headers`, and the agent code itself is responsible for extracting `sub` from it. | This is a real, small implementation task (decode a JWT payload inside the agent), not a built-in AgentCore feature — worth recording explicitly since it was initially assumed to be automatic during design, and a future reader of `agent.py` should understand why this decoding step exists there rather than being handled by the Runtime. |
| 35 | JWT decoding in agent code | `PyJWT`, decode-only (`jwt.decode(token, options={"verify_signature": False})`), added to `agent/requirements.txt`. No signature re-verification inside the agent. | The Runtime's `customJwtAuthorizer` has already cryptographically verified the token (signature, expiry, issuer, audience/client/scopes) before the agent process ever runs — decoding again inside the agent is purely to read the `sub` claim out of a token already known to be valid, not a second trust boundary. Hand-rolling base64url + JSON decoding was considered and rejected: it's a small amount of code but has enough real edge cases (padding, encoding) that a well-known, actively maintained library is preferable to reinventing it for no real savings. |
| 36 | Gateway observability | Added CloudWatch application logs + X-Ray traces for `TravelAgentGatewayStack`'s Gateway (`gateway_stack.py`'s `_configure_observability()`): a dedicated log group, a delivery source/destination/delivery pair each for `APPLICATION_LOGS` and `TRACES`, per AWS's documented SDK pattern for gateway resources. | Confirmed via AWS docs that, unlike Runtime, AgentCore does not provision any log/trace destination for Gateway resources automatically — without this, a failing tool call surfaces to the end user only as an opaque "An internal error occurred" with no way to see the real upstream error, target name, or request/response bodies. Enabling this let a real Gateway target failure be root-caused directly from CloudWatch logs (specific error message, failing target, trace/span IDs) rather than guessed at. |

## 2b. Hosted Web UI on ECS Fargate (added 2026-08-27, supersedes decision #19/#26-35's Runtime-auth half)

Adding `TravelAgentWebStack` — a shared, always-on hosted deployment of the
web UI, as an alternative to everyone running `web/server.py` locally
(decision #19, still supported and unchanged for local use). This section
also reverts the Runtime-auth half of decisions #26-35 (the Okta-JWT
bearer-token cutover) back to IAM — decision #37 explains why — while
decisions #26-35's other artifacts (the ALB's own separate OIDC login,
`sanitize_actor_id()`'s rationale, etc.) carry forward in adapted form.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 37 | Runtime auth model, reverted (superseded by decisions #63-75, §2f, when `WEB_RUNTIME_OIDC_*` is configured — this row's `using_iam()` default remains the actual behavior whenever that config is absent) | `RuntimeAuthorizerConfiguration.using_iam()` — full reversion of decisions #26-35's Okta-JWT bearer-token cutover. Every caller (the CLI; now also `TravelAgentWebStack`'s ECS task) invokes `InvokeAgentRuntime` with its own AWS credentials, SigV4-signed. `actor_id` moves from a server-side-decoded JWT `sub` claim to an explicit field in the invocation payload (`agent/agent.py`'s `get_actor_id(payload)`) — the caller is trusted to supply the correct value, the same way it's already trusted with IAM access to invoke the Runtime at all. | The entire reason decisions #26-35 added JWT auth was to give the CLI and local web UI a way to distinguish real human identities with no other authentication layer available to them. Once `TravelAgentWebStack`'s OIDC-authenticated ALB exists, that job is already done *before* a request ever reaches this project's own code — the ALB is a real, independently-operated identity boundary, and the Runtime's own JWT authorizer became redundant plumbing duplicating a check the ALB already performs. Worse, the JWT-bearer-token path was *only* usable by a caller that could complete an interactive Okta login (the CLI's local subprocess flow) — it had no answer for how a non-interactive ECS task would authenticate as a specific human user, and "the container performs a service-to-service OAuth client-credentials grant just to talk to its own account's Runtime" would have been meaningfully more complex than simply trusting the task's own IAM identity, which every other AgentCore-Runtime-calling component in this project already does. IAM auth is not a security regression here: the caller that can reach the Runtime with valid credentials is either (a) this project's own ECS task, sitting behind the ALB's real OIDC gate, or (b) a local CLI user who already has full IAM access to this account's resources — there is no untrusted public path that can supply an arbitrary `actor_id`. |
| 38 | Web UI hosting: compute, load balancing, and auth | ECS Fargate (0.5 vCPU/1GB per task, autoscaling 1-3 on CPU 60%) behind an internet-facing Application Load Balancer with an `authenticate-oidc` listener action (Okta as the IdP) placed in front of a `forward` action to the Fargate target group. `web/server.py`'s `actor_id_from_oidc_header()` independently re-verifies the ALB's signed `x-amzn-oidc-data` header (ES256, fetching the signing public key from ALB's per-region `public-keys.auth.elb.<region>.amazonaws.com/<kid>` endpoint, and checking the JWT header's `signer` field against this exact ALB's ARN) before trusting any claim in it — per AWS's own documented security requirement, not an optional hardening step. | User-specified (compute type, autoscaling, and OIDC-on-ALB were all given constraints up front, not derived). Independent signature re-verification (rather than trusting the header blindly) matters specifically because a misconfigured security group, or any bug that let a request reach the Fargate task without actually passing through the ALB's listener rule, could otherwise let a caller forge an arbitrary `actor_id`/`sub` claim and read or write another user's long-term Memory — the same class of risk `sanitize_actor_id()` was originally added for in decisions #26-35, now defended at the transport-trust layer instead of (or in addition to) the character-set layer. |
| 39 | Web UI hosting: networking | A new, dedicated VPC (2 AZs, public + private-with-egress subnets, one NAT Gateway per AZ) — not a pre-existing/shared VPC. Fargate tasks sit in the private subnets with no public IP, reachable only from the ALB's security group; the ALB itself sits in the public subnets, internet-facing. | Matches this project's existing pattern of every stack provisioning its own resources rather than depending on external infrastructure (no other stack in this project references a pre-existing VPC). Keeping compute off the public internet even though the security group would already block unsolicited inbound traffic is the standard defense-in-depth posture for a real multi-tenant, internet-reachable service — a materially different risk profile than the local-only web UI (decision #19) or the CLI, both of which run entirely under the operator's own control. |
| 40 | Web UI hosting: TLS and DNS scope | The caller supplies an existing ACM certificate ARN (`WEB_CERTIFICATE_ARN`); this stack does not provision, import, or validate a certificate itself, and does not create any Route 53 record — the ALB's own generated DNS name is used as-is, and pointing a real domain at it is left entirely to the caller. | User-specified: explicitly out of scope for this stack, to avoid this CDK app guessing at a domain/hosted-zone configuration it has no visibility into (a wrong guess here has real blast radius — a misconfigured Route 53 record or a wrongly-scoped ACM certificate request touches shared DNS/PKI state, not just this project's own resources). |
| 41 | Container build and deploy mechanism | `ecs.ContainerImage.from_asset()` — CDK builds and pushes the Docker image (from a new `web/Dockerfile`) to an auto-created ECR repository as part of `cdk deploy`, with the same manual (non-pipeline) deploy workflow used by every other stack in this project. The asset's Docker build context is the whole repo root (needed since the Dockerfile `COPY`s both `web/` and `cli/agent_client.py`, which are siblings, not nested under one directory), with explicit `exclude` patterns for `cdk/cdk.out`, `.venv`, `.git`, and `__pycache__` — omitting these caused a real, confirmed `ENAMETOOLONG` failure during this stack's own development, since `cdk synth` re-copies its own previous output into the new asset staging directory without them (a self-referential recursive copy). | Matches `RuntimeStack`'s existing `AgentRuntimeArtifact.from_code_asset()` pattern for `agent/` — one `cdk deploy` builds and deploys everything, no separate manual image build/push step or CI/CD pipeline, consistent with every other stack's deploy story in this project (user-specified: explicitly deferred a real pipeline as future scope, not needed for a small-group internal tool). |
| 42 | Web UI hosting: IAM scope (superseded in part by decision #78 — `runtime.grant_invoke()` removed) | The ECS task role is granted only `memory.grant_full_access()` and `runtime.grant_invoke()` — scoped to the exact Memory and Runtime resource ARNs this deployment uses, no wildcard `bedrock-agentcore:*` grant. | User-specified, and consistent with this project's existing least-privilege convention (e.g. `GatewayStack`'s `_grant_web_search_invoke()` scoping to a literal resource ARN rather than a service-wide wildcard) — this task role is a real blast-radius boundary for a genuinely multi-tenant, internet-reachable service, unlike the CLI's use of the operator's own broader local credentials. |
| 43 | Web UI hosting: sizing/traffic assumptions | Fargate: 0.5 vCPU/1GB per task, autoscaling 1-3 tasks on CPU utilization target 60%. ALB idle timeout raised to 180s (from the 60s default); Fargate `health_check_grace_period` matched to the same 180s. Deployment configured with `circuitBreaker` (rollback on failure) and `minHealthyPercent=100` (matches `desired_count=1` — without this, a single-task deployment could briefly drop to zero running tasks mid-rollout). | User-specified: a genuinely multi-tenant identity model (decision #38's per-user OIDC-derived `actor_id`) but explicitly *not* a high-traffic-volume design target — sized conservatively and easy to scale up later via a CDK property change, not because of any measured or expected load. The 180s idle-timeout/grace-period value is not arbitrary: real itinerary-generation turns have been observed taking up to ~56s (see PLAN.md Phase 7's `MaxTokensReachedException` fix notes), close enough to the ALB's 60s default that a long, tool-heavy turn could otherwise have its connection killed by the ALB mid-stream. |
| 44 | Web UI hosting: CI/CD scope | Plain manual `cdk deploy TravelAgentWebStack`, same as every other stack — no CodePipeline/Amazon Pipelines automation. | User-specified: explicitly deferred as separate, larger scope (source triggers, approval gates, rollback strategy) that wasn't asked for — matches this project's existing all-manual-deploy pattern for the four base stacks. |

Explicitly still true, carried over from decisions #26-35: the ALB's OIDC
login (decision #38) is a genuinely separate Okta app/config from anything
the CLI uses (decision #27's "dedicated Okta app" framing, now applied to
the ALB instead of the Runtime) — the CLI still has no ALB in front of it
and never touches Okta at all after this change. `sanitize_actor_id()`'s
rationale (an OIDC `sub` claim is not guaranteed to satisfy AgentCore
Memory's `actorId` character-set pattern; a real Okta org's `sub` was an
email address) is unchanged — it now runs inside `web/server.py`'s
`actor_id_from_oidc_header()` on the ALB's claims instead of on a
Runtime-forwarded bearer token, and `agent/agent.py`'s `get_actor_id()`
also sanitizes defensively regardless of caller.

Explicitly still true, carried over from the superseded decisions:
IAM auth is **removed**, not kept as a parallel option (unlike a typical
"add an alternative" change) — see decision #26. The web UI's local-only,
single-user, no-hosting scope (decision #19) is explicitly **not**
reopened by this change — see decision #30.

## 2c. Post-deploy hardening (added 2026-08-27/28)

Three fixes made after `TravelAgentWebStack` was first deployed and
exercised against real traffic — each found by actually using the live
deployment, not anticipated during §2b's design pass.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 45 | Fargate CPU architecture | `runtime_platform=ecs.RuntimePlatform(cpu_architecture=ecs.CpuArchitecture.ARM64, ...)` set explicitly on the Fargate task definition, matching a native-`arm64` Docker build. | Building for Fargate's default (`X86_64`) from an Apple Silicon (`arm64`) development machine requires cross-architecture emulation (Docker's QEMU-based `buildx`), which was confirmed in practice to hang indefinitely pushing the built image to ECR under this environment's emulation layer — never completing, not just slow. Building and running natively as `arm64` end-to-end (Fargate does support `arm64` tasks) sidesteps the emulation path entirely. Not a general-purpose fix for x86-only dev machines — this is specifically for building on Apple Silicon. |
| 46 | Debug endpoint for identity verification | `GET /api/whoami` added to `web/server.py`, returning `{"sub": ..., "actor_id": ...}` from the same `actor_id_from_oidc_header()` path every other endpoint uses, protected by the same ALB OIDC gate (no separate/weaker auth). | Diagnosing an X-Ray trace that appeared to show the wrong `actor_id` (traced to a stale client-side `session.id` prefix, not an actual identity bug — see decision #47) required a fast way to confirm "what does the server actually resolve my identity to, right now" without reasoning through code paths or grepping logs. A tiny, always-available endpoint answers that directly than re-deriving it manually each time. |
| 47 | Client-side auth-expiry handling | `web/static/app.js` treats any `fetch()` rejection (and, defensively, an explicit 401) on a call to a protected `/api/*` route as "session expired," and responds with `window.location.reload()` rather than showing a generic error — capped at 2 reload attempts per browser session (`sessionStorage`) to avoid a reload loop if the backend is genuinely broken rather than just needing reauthentication. | The ALB's `authenticate-oidc` listener (`OnUnauthenticatedRequest=AUTHENTICATE`, decision #38) responds to *any* request with a missing/expired session cookie — including this app's own `fetch()` calls, not just top-level page loads — with an HTTP 302 to Okta. A real page navigation follows that redirect with no restriction, but `fetch()` cannot: the browser still applies its CORS check to the final response in the redirect chain (Okta's `/authorize` page, which has no `Access-Control-Allow-Origin` header), so the request rejects with a generic error instead of ever exposing a 302 or a 401 to the caller. Confirmed directly against the deployed ALB with `curl`: a request with zero cookies returns 302, not 401 — ruling out `OnUnauthenticatedRequest=deny`'s documented "clean 401 for AJAX" behavior, which does not apply to this ALB's configuration. A first attempt assumed the 401 case applied and only checked `res.status`; it had no effect, since this ALB's redirect-always mode never produces an observable 401 for `fetch()` to check. The working fix instead reacts to the rejection itself, since a same-origin `/api/*` fetch can only fail this way for a genuine network error or this redirect-blocked-by-CORS case, and reload is the correct response to either. `on_unauthenticated_request` was deliberately left as `AUTHENTICATE` rather than switched to `deny` — `deny` would also change behavior for a plain top-level page load with no cookie (returning a raw 401 instead of redirecting to Okta), breaking the primary "just open the site and get sent to log in" flow that decision #38 relies on. |

## 2d. CLI removal (added 2026-08-28)

The local CLI REPL client (`cli/chat.py`, plus its shared transport module
`cli/agent_client.py`) is removed entirely. The hosted web UI
(`TravelAgentWebStack`, §2b) is now the only supported way to use this
agent — there is no other client, local or otherwise.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 48 | CLI removal scope | Full removal: `cli/` deleted entirely (`chat.py`, `agent_client.py`, `requirements.txt`), along with its dedicated tests (`tests/test_chat.py` deleted outright; `tests/test_agent_client.py`'s coverage — none of which was CLI-specific — moved to `web/tests/test_agent_client.py` against the relocated module). `agent_client.py` itself is not deleted, since `web/server.py` depends on it for session-ID construction and the actual `InvokeAgentRuntime` call — it moves to `web/agent_client.py`, becoming a same-directory sibling instead of a cross-directory import via a `sys.path` hack. | Once the hosted web UI (§2b) existed and worked, the CLI stopped serving any purpose decision #10's original "testing/invocation clients" rationale needed — it was already redundant with the AgentCore console's test chat for a quick Runtime sanity check, and with the web UI for actual end-user conversations. Keeping it around meant maintaining a second, less-capable client (no streaming visibly rendered, no diagnostics panel, no conversation history) with its own auth story (a raw `--actor-id` flag, decision #15/#37) that diverged from the real identity model (§2b's ALB-derived `actor_id`) every other caller now uses. Removing it outright — rather than leaving it as a "power user" escape hatch — was a deliberate simplification: a second supported client is an ongoing maintenance cost (keeping it in sync with `agent_client.py`'s interface, its own test suite, its own docs) for a capability (local IAM-credentialed access) nothing in this project's actual usage needed once the web UI existed. |
| 49 | Docker build context simplification | `web/Dockerfile` now `COPY`s only from `web/` itself (`COPY . .`, `ENTRYPOINT ["python", "server.py"]`) instead of `web/` plus a sibling `cli/agent_client.py`. `cdk/stacks/web_stack.py`'s `ecs.ContainerImage.from_asset()` build context narrows from the whole repo root to `web/` alone, and its `exclude` list (previously needed to keep `cdk/cdk.out`/`.venv`/`.git` out of a repo-root build context) is removed entirely, since `web/` never contained any of those directories in the first place. | This was a real, independent simplification unlocked by the CLI removal, not just incidental cleanup: the *only* reason the Docker build context was ever the whole repo root was to let the Dockerfile reach across to the sibling `cli/agent_client.py` file (see decision #41's original rationale) — with `agent_client.py` now living inside `web/`, that cross-directory reach no longer exists, so there's no reason for the build context to be anything wider than `web/` itself. A narrower build context is strictly better: smaller build context to hash/upload, no risk of ever needing the `cdk.out` self-referential-copy workaround again (decision #41's `ENAMETOOLONG` bug can't recur if `cdk.out` is never in the context to begin with). |

## 2e. Phase 1 auth rearchitecture: app-level OIDC replaces the ALB (added 2026-08-28)

Removes the ALB's `authenticate-oidc` listener action (decision #38) —
the ALB reverts to a plain TLS-terminating, non-authenticating load
balancer — and moves the entire OIDC login flow into `web/server.py`
itself (new `web/auth.py` module). The motivating requirement (recorded
in discussion, not yet implemented — a future phase): giving the
AgentCore Gateway an inbound JWT with a real, cryptographically-signed
`sub` claim, so its Policy Engine and rate-limiting features can key
authorization/throttling decisions on genuine per-user identity rather
than the Gateway's current single IAM-role caller identity. That requires
a live, forwardable/exchangeable signed token somewhere in the request
path — the ALB's `x-amzn-oidc-data` header is fully decoded and discarded
by `web/server.py` today (only a plain string, `actor_id`, survives past
it), so there is no live token left to propagate. Running the OIDC flow
in the app itself, rather than the ALB, means a real Okta-issued token
exists in this process for as long as a session is active, available for
a later phase's Runtime→Gateway (or Server→Runtime) on-behalf-of token
exchange.

This section is scoped to *only* the ALB→app-level auth migration.
Propagating a JWT to the Gateway, adding the Gateway's Policy Engine, and
any OBO/token-exchange work are explicitly out of scope here — later
phases, not yet designed in detail.

**Update (2026-08-30):** the JWT-propagation and OBO-token-exchange half
of this motivating requirement is now implemented — see §2g. The
Gateway's Policy Engine and rate-limiting features themselves remain
unbuilt and out of scope; §2g's own scoping note (decision #87) is
explicit that this phase is plumbing only, with no `sub`-based
authorization behavior added yet.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 50 | Okta app | A new, dedicated Okta application (confidential client, its own client ID/secret) — separate from the ALB's former app (decision #38) and from the CLI-era `okta-claude-code-token-helper` app (decision #27, both now unused/removed). | Matches this project's established convention (decisions #27, #38) of never sharing an Okta app registration across trust boundaries — a token minted for one system must not silently authenticate to another. |
| 51 | Token scopes | `openid offline_access`. No `profile`/`email`, no group/role claims requested yet. | `offline_access` is required to get a refresh token back from this Okta org's authorization server at all — silent refresh (decision #55) has nothing to work with otherwise. Group/role claims are deferred: they'd only matter for a future Gateway-policy phase, and shaping this Okta app around an undesigned future requirement was rejected — scopes are cheap to add later without touching this phase's design. |
| 52 | Refresh token rotation | This Okta app is configured to return the *same*, non-rotating refresh token on every refresh (not Okta's alternative rotate-on-use behavior). | Avoids an entire class of concurrency bug: a rotating refresh token invalidates itself the instant it's used, so two requests racing to refresh with the same (now-stale) token would otherwise need server-side coordination (a lock/mutex) to avoid one of them failing — directly working against this project's explicit goal of keeping the Fargate tasks stateless. With a fixed refresh token, no such coordination is needed; any task can refresh independently at any time. |
| 53 | Session cookie: confidentiality | KMS envelope encryption (`GenerateDataKey`/`Decrypt`, AES-256-GCM for the actual payload cipher) — not a signed-but-plaintext cookie like the ALB's original `x-amzn-oidc-data`. A single cookie holds the AES-GCM nonce, ciphertext, and the KMS-*encrypted* data key together; the raw AES key is only ever held in process memory for the duration of one encrypt/decrypt call, never persisted. | The cookie now carries real bearer credentials (an Okta access token and refresh token) rather than just identity claims — a signed-but-readable cookie would let anyone with browser access to it (devtools, a malicious extension, a leaked log line) read the raw tokens directly. Envelope encryption via KMS (rather than a self-managed static symmetric key) shifts key custody, rotation, and access-auditing entirely to AWS, via the same IAM-role-authorizes-the-call pattern already used elsewhere in this project (e.g. `bedrock-agentcore:InvokeAgentRuntime`) — no raw key material to generate, store, or rotate ourselves. |
| 54 | KMS key rotation strategy | Automatic annual rotation enabled on the KMS key, combined with per-session data keys (decision #53) — each cookie carries its own encrypted data key, so it never depends on which KMS key *version* encrypted it. | Makes key rotation a non-event for already-issued sessions: rotating (or even fully replacing) the KMS key only affects newly-generated data keys going forward, and AWS's own key-rotation guarantee keeps old key material available for `Decrypt` regardless. No session is ever forcibly invalidated by a rotation. |
| 55 | Refresh timing | Synchronous, inline refresh: a request arriving with an expired access token triggers an immediate call to Okta's token endpoint as part of handling that same request, and the resulting new access token is written back into the session cookie on that same response (`Set-Cookie`) before the caller ever sees a failure. | The alternative (deferred/best-effort cookie rewrite, tolerating a stale cookie until some later request) has no real benefit here — refreshing has to happen synchronously in-request regardless (the current request needs a valid token to actually call the Runtime), so writing the resulting cookie on the same response is free; deferring it would only guarantee a second, wasted refresh call on the very next request. |
| 56 | 401 vs. redirect split | `web/auth.py`'s `is_browser_navigation()` inspects the `Sec-Fetch-Mode` header (falling back to `Accept: text/html` for older/unusual clients) to distinguish a top-level page load from a `fetch()`/XHR call. A top-level load with no valid session gets redirected straight to Okta (302); a `fetch()` call gets a clean, real `401`. | This is a genuine improvement over decision #47's ALB-era workaround, not just a side effect: the ALB's `authenticate-oidc` action, once configured for `OnUnauthenticatedRequest=AUTHENTICATE`, had no mode that returned a real 401 to an AJAX call — it always 302'd, which a same-origin `fetch()` cannot follow (the browser's CORS check blocks the final response in that redirect chain). That forced `web/static/app.js` to treat *any* fetch rejection as a possible auth failure and blindly retry via a full-page reload, capped at a small retry count to avoid a loop. Now that this server's own code decides the response, returning a real 401 for `fetch()` calls removes that entire class of ambiguity — `app.js`'s `res.status === 401` checks (already present, previously dead code under the ALB) are now the sole, reliable auth-expiry signal, and the reload-loop-guard machinery (`AUTH_RELOAD_KEY`, capped retries) is removed as no longer needed. |
| 57 | CSRF/PKCE protections | A short-lived (10-minute), separate `travel_agent_pending_login` cookie carries the OAuth `state` value and the PKCE code verifier across the redirect round-trip to Okta and back; deleted as soon as `/oauth2/callback` consumes it. PKCE (S256) is used even though this is a confidential client with a client secret. | `state` verification is the standard CSRF defense for OAuth redirect flows — without it, an attacker could trick a victim's browser into completing an attacker-initiated login. PKCE is not solely a public-client mitigation: current OAuth security guidance (RFC 9700) recommends it universally, including for confidential clients, since it independently defends against authorization-code interception/injection regardless of whether the client can hold a secret — a small amount of extra code for a real, no-downside hardening. This pending-login cookie itself is deliberately *not* encrypted (only the session cookie, decision #53, is) — nothing in it is a bearer credential; its security property comes from `state`'s unpredictability and exact match, not from the cookie's own confidentiality. |
| 58 | `sub` extraction from the access token | `web/auth.py` decodes Okta's returned access token as a JWT, without verifying its signature, to read the `sub` claim. | Safe specifically because this token was just obtained directly from Okta's own token endpoint over TLS, by this server's own code (the authorization_code or refresh_token grant) — never supplied by an untrusted caller — so there is no scenario where an attacker-controlled token reaches this decode step. This is a materially different trust context than the old ALB header verification (decision #38's `actor_id_from_oidc_header()`), which had to defend against a forged/replayed header reaching the container from an unverified source; that entire signature-verification mechanism (ES256 against ALB's public-keys endpoint) is removed along with the ALB's OIDC responsibility. **Verified 2026-08-29** against the real, registered Okta app (decision #50): this org's access tokens are JWT-shaped and carry a `sub` claim, confirming the assumption this decode step relies on. |
| 59 | ALB/CDK scope of this change | The ALB's `authenticate-oidc` listener action, `on_unauthenticated_request`, and every `oidc_*`/`WEB_OIDC_USER_INFO_ENDPOINT` construct prop tied to it are removed from `cdk/stacks/web_stack.py`; the listener's `default_action` becomes a plain `ListenerAction.forward([target_group])`. A new `SessionCookieKey` (KMS) and `OidcClientSecret` (Secrets Manager) resource are added, both scoped via IAM grants to only this stack's own `WebTaskRole`. The ALB itself (TLS termination, health checks, the 180s idle timeout of decision #43, autoscaling target-group routing) is unchanged — only its identity-checking responsibility moves into the container. | The ALB remains the right tool for TLS/routing/health-checks regardless of who handles auth — replacing the ALB itself was never the goal, only its `authenticate-oidc` action. Storing the Okta client secret in Secrets Manager (rather than passing it as a plain environment variable, as the ALB's own listener-rule property implicitly did before) reflects that this container, not the ALB, now holds and uses this secret directly on every login/refresh — a materially different exposure than a value the ALB used internally and never exposed to the container at all. |
| 60 | Logout | No `/logout` route added in this phase. | Not a regression: neither the ALB's `authenticate-oidc` action nor any prior version of this app ever exposed an explicit logout affordance — this phase's scope is replacing the ALB's OIDC mechanics with an equivalent, not adding new capability. A real logout (clearing the local session and optionally ending the Okta-side session) is a reasonable future enhancement, deliberately deferred rather than folded into this already-large change. |

### Post-deploy fix (found 2026-08-29, same day as first deploy)

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 61 | `GET /` had no auth check at all | Added `_resolve_auth(request)` (with `apply_refreshed_cookie_if_needed()`) to the `/` route, matching every other protected route. | Real bug, caught by a live `curl` check against the deployed ALB immediately after the first Phase 1 deploy — `GET /` with `Sec-Fetch-Mode: navigate` and no session cookie returned `200` (serving `index.html` unconditionally) instead of the expected `302` to Okta. Root cause: under the old ALB model, `/` never needed its own auth check because the ALB gated every request at the network edge before any of them reached the container at all; that assumption silently carried over into this phase's `create_app()`, where `index()` was never updated to call `_resolve_auth()` like `/api/whoami` and the others. Not caught by the automated test suite before this session's redeploy, since no test asserted on `/`'s auth behavior at all (the pre-existing `test_index_serves_html` used an already-authenticated test client, which passed regardless of whether the route itself checked anything) — `test_index_route_requires_auth`/`test_index_route_returns_401_for_fetch_style_call_with_no_session` were added as regression coverage after this fix. `/favicon.ico` was checked and correctly left unauthenticated — a non-sensitive static asset browsers request unconditionally before any login state is known, unlike `/`'s real page content. |
| 62 | `redirect_uri` uses a caller-supplied hostname, not the ALB's own DNS name | Added a new required `web_hostname` constructor parameter to `WebStack` (env var `WEB_HOSTNAME`), used to build `redirect_uri` (`https://{web_hostname}/oauth2/callback`) instead of `self.load_balancer.load_balancer_dns_name`. | Found immediately after the first live deploy: the user's actual Okta app was registered with a friendly custom domain (`travel-agent.kenkitts.people.aws.dev`, already pointed at this ALB per decision #40's "DNS is the caller's responsibility" — confirmed via a real ACM wildcard certificate, `*.kenkitts.people.aws.dev`, already `InUse: true` on this exact ALB) — but the code as originally written computed `redirect_uri` from the ALB's own raw generated DNS name, which a live `curl` against `/` confirmed in the actual `/authorize` URL. Registering a domain in Okta that the running code never actually sends as `redirect_uri` is a guaranteed `redirect_uri mismatch` failure at every login attempt, not a cosmetic mismatch — this stack has no way to detect that mismatch itself (it doesn't validate DNS or call out to Okta at synth/deploy time), so it fails silently until a real login is attempted. `web_hostname` is deliberately a new, explicit, caller-supplied stack input (not derived from the ACM certificate's domain name, which could be a wildcard covering many possible hostnames, or from any AWS resource) — this project has consistently treated real-domain/DNS decisions as the caller's exclusive responsibility (decision #40) rather than something CDK should infer or guess. |
## 2f. Phase 2 auth rearchitecture: RFC 8693 token exchange replaces IAM/SigV4 for the Runtime (added 2026-08-29)

Motivation: Phase 1 gave the web server a real, verified Okta identity for
every logged-in person, but the web server still called AgentCore Runtime's
`InvokeAgentRuntime` with its own ECS task's IAM credentials (decision #37) —
`actor_id` was passed as a plain payload field the Runtime simply trusted,
not something the Runtime itself could verify came from a genuine,
currently-valid user session. This phase closes that gap: the web server now
exchanges the user's live Okta access token for a second, Runtime-audienced
JWT (RFC 8693 OAuth 2.0 Token Exchange) and presents *that* to the Runtime,
whose own JWT authorizer independently validates it. This does **not**
change the Gateway's auth (still IAM/SigV4, deliberately — decision #64
below) and is **not** the same thing as the Gateway-JWT-propagation/Policy-
Engine work floated during Phase 1's design discussion — that remains a
separate, undesigned future phase.

Two AWS-documented facts, confirmed directly against AWS's own
documentation before any code was written (not assumptions), reshaped this
design significantly partway through:

1. **JWT and IAM inbound auth are mutually exclusive per-Runtime, not
   additive.** ("An AgentCore Runtime can support either IAM SigV4 or JWT
   Bearer Token based inbound auth, but not both simultaneously.") Switching
   the Runtime to JWT is a hard replacement of decision #37, not a parallel
   option alongside it.
2. **boto3 cannot invoke a JWT-authorized Runtime at all.** ("Since boto3
   doesn't support invocation with bearer tokens, you'll need to use an HTTP
   client like the requests library.") `web/agent_client.py`'s
   `stream_agent_events()` required a real rewrite — a raw HTTPS POST with
   `Authorization: Bearer`, manual SSE parsing — not a small patch.
   (https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/runtime-oauth.html)

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 63 | Exchange mechanism | Real RFC 8693 OAuth 2.0 Token Exchange (`grant_type=urn:ietf:params:oauth:grant-type:token-exchange`) against a dedicated Okta "API Services" app configured for the Token Exchange grant type — not direct forwarding of the Phase 1 web-login app's own access token, and not an AWS-side re-signing/wrapping mechanism. | User's explicit choice. Confirmed via Okta's own documentation (`developer.okta.com/docs/guides/set-up-token-exchange`) that this is a real, supported Okta feature (Okta's own "On-Behalf-Of Token Exchange" guide) requiring a genuinely separate app registration — not something "just on" by default for any Okta app. |
| 64 | Gateway auth scope | Left completely untouched — still IAM/SigV4, via the Runtime's own execution role, exactly as before. | The agent process's call to the Gateway (for MCP tools) is a separate, unrelated trust boundary from the web-server-to-Runtime hop this phase changes — confirmed by reading `agent/agent.py`, which SigV4-signs its own outbound Gateway calls independently of how the Runtime's *inbound* invocation was authenticated. Touching Gateway auth was explicitly out of scope for this phase (the original "give the Gateway a real per-user JWT for its Policy Engine" motivating goal from the Phase 1 checkpoint remains a separate, undesigned future phase). |
| 65 | Okta authorization server | A second, genuinely different Okta authorization server than the Phase 1 web-login app's, with Okta "Trusted servers" configured between them (user confirmed already set up in their Okta org). | User's existing Okta org setup — not something this project's code could configure. `RuntimeOidcConfig` is a deliberately separate dataclass from Phase 1's `OktaConfig`, not additional fields on it, since it describes a different issuer/token_endpoint/client_id/client_secret entirely. |
| 66 | Cookie caching for the exchanged token | Cached in the session (via a second, independent cookie — see #67), refreshed inline based on its own `exp` — same synchronous-inline-refresh pattern as Phase 1's session cookie (decision #10), applied to a second token. | Avoids a redundant Okta round-trip on every single `/api/chat` call (which would add latency to every streamed chat turn) while preserving the stateless-server property Phase 1 deliberately chose (decision #6-8) — no server-side session store, any ECS task can handle any request. |
| 67 | Second cookie vs. folding into the existing session cookie | A **separate, independent cookie** (`travel_agent_runtime_token`), not additional fields merged into Phase 1's `travel_agent_session` cookie. | A real, measured envelope-encrypted-cookie-size check (not a guess) found that stacking a second full JWT into the existing session cookie would push its total size well past the ~4KB practical per-cookie browser limit that Phase 1's decision #8 had already flagged as an accepted risk at the *current*, single-token size. Splitting into two independently-sized cookies avoids that size cliff entirely. The user explicitly considered and rejected two alternatives first: no caching at all (re-exchange every call — rejected once the size math made caching-via-second-cookie clearly preferable) and reusing headroom from removing the OIDC ID token from the existing cookie (rejected once it was confirmed the ID token was never being stored there in the first place — there was no such headroom to reclaim). |
| 68 | Encryption of the second cookie | Same KMS envelope encryption as the session cookie (same KMS key, same `_KmsEnvelopeCodec` scheme — refactored out of `SessionCookieCodec` into a shared base once a second cookie needed the identical treatment) — not plaintext or signed-only. | This token is exactly as much a real, usable bearer credential (it authenticates as this specific user to the Runtime, scoped to `runtime:invoke`) as the Okta access/refresh tokens Phase 1 already decided must be encrypted, not just signed (decision #6's rationale applies identically here). |
| 69 | Missing/expired Runtime-token cookie behavior | Never itself an auth failure — silently triggers a fresh exchange (using the still-valid Okta access token from the main session) rather than forcing a full re-login (401/redirect to Okta). | This cookie is a *derived* artifact of the main session, not an independent credential the user "logs in" to get — its absence just means it needs to be (re-)minted, which the server can always do as long as the underlying session is valid. Treating its absence as a full auth failure would force unnecessary re-logins on ordinary events (first Runtime call after login, or the two tokens simply having different lifetimes), with no security benefit. |
| 70 | Where the exchange logic lives | Only in `/api/chat`'s route handler — not folded into the shared `_actor_id()`/`_resolve_auth()` helpers every protected route uses. | `/api/whoami` and the conversation-history endpoints talk to AgentCore Memory directly via the ECS task's own IAM role and have no reason to ever need a Runtime-audienced JWT — running the exchange there would be pure waste (an unnecessary KMS operation and potentially an unnecessary Okta network call) on every call to routes that don't touch the Runtime at all. |
| 71 | Token-exchange-call failure handling | A genuine failure of the exchange call itself (Okta's token endpoint down, misconfigured scope/audience) surfaces as a clean `502` — matching this file's existing convention for downstream-dependency failures (e.g. `list_conversations()`'s AgentCore Memory `ClientError` handling) — never conflated with a session-invalid `401`/redirect. | A user's *session* can be perfectly valid while Okta's token-exchange endpoint has a transient problem; sending them through a full re-login for a purely upstream failure would be misleading (a redo of the login wouldn't fix it) and confuses two genuinely different failure causes. |
| 72 | Runtime resource-based IAM policy | None added — no defense-in-depth IAM restriction layered on top of the JWT authorizer. | AWS's own docs describe JWT-based inbound auth as the complete, designed access-control mechanism for exactly this scenario (network-facing, multi-user access) — the actual thing worth being careful about is the Runtime's own `allowedAudience`/`allowedClients`/`allowedScopes` configuration being correct, not a parallel, largely redundant IAM layer on a Runtime whose real gate is OIDC token validation, not IAM at all once JWT is configured. |
| 73 | Retry/backoff for the new HTTPS call | None added — parity-only with today's error handling (non-2xx → a clean `RuntimeError`/in-band SSE error event), no explicit retry/backoff for the documented-retryable cases (`ThrottlingException`, the 409 `RetryableConflictException`). | boto3 previously provided this transparently; dropping boto3 (a hard requirement once the Runtime is JWT-authorized — see this section's opening AWS-docs findings) silently drops that behavior too. Reimplementing retry/backoff properly (error classification, backoff timing, idempotency for a streaming call) is a genuinely separate, non-trivial piece of scope the user explicitly deferred rather than bundling into this already-large auth change. **Known, intentional gap** — not an oversight — worth revisiting separately. |
| 74 | Exchange-app credential/config storage | Follows Phase 1's exact pattern: plain CDK/container args for non-secrets (issuer, token endpoint, audience, scope, client ID), one new Secrets Manager secret (`RuntimeOidcClientSecret`) for the exchange app's client secret specifically, fetched once at container startup — not a single consolidated JSON-blob secret, and not folded into Phase 1's existing `OidcClientSecret`. | Directly mirrors Phase 1's already-proven, identical-shape pattern for structurally identical data (a second Okta app's config) — no reason to invent a different shape, and consistency makes the two Okta app configs easy to compare side by side in `.env`/`.env.template`/the CDK stack. |
| 75 | `RuntimeStack`'s JWT authorizer: required or optional | **Optional** — defaults to today's `using_iam()` when the new `WEB_RUNTIME_OIDC_*` env vars are absent, switches to `using_jwt(...)` only when present. `RuntimeStack` gets no new *required* constructor parameters. | `RuntimeStack` has zero dependency on `WebStack`/Okta configuration today — it deploys and is independently testable (e.g. directly via the AgentCore console, per the README's documented use case) with no web-hosting prerequisites at all. Making Okta config a hard requirement here would silently break that standalone-testability path the moment this phase shipped, for a project whose Runtime is, in practice, only ever actually invoked by the web server — there's no reason to force every deployment of `RuntimeStack` to also stand up Okta config it might not need yet. **Confirmed live (2026-08-29), after deploying with `WEB_RUNTIME_OIDC_*` set**: this optionality is at the *code/CDK level* only — it lets an operator choose not to deploy JWT config at all, and RuntimeStack stays IAM/console-testable in that case. It does **not** mean console testability survives *once* JWT is actually configured; AWS's own mutual-exclusivity behavior (this section's opening finding) applies in full the moment `WEB_RUNTIME_OIDC_*` is set — the user reported the AgentCore console's test chat now requires a valid JWT for console access and no longer works for this Runtime. This decision was never intended to (and does not) let both auth modes coexist for one already-JWT-configured Runtime — see PLAN.md Phase 13's "Remaining manual steps" for the confirmed regression and its accepted-tradeoff framing. |

### Post-deploy fixes (found 2026-08-29/30, live after the first Phase 2 deploy)

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 76 | Okta app-level DPoP requirement blocked the exchange call entirely | User disabled "Require Demonstrating Proof of Possession (DPoP) header in token requests" on the exchange app in the Okta Admin Console. No code change. | Real live failure: `exchange_token_for_runtime()`'s POST to Okta's `/token` endpoint returned `400 invalid_dpop_proof: The DPoP proof JWT header is missing`. DPoP (RFC 9449) is a genuinely separate, non-trivial Okta app-level feature (a distinct asymmetric-key-pair-plus-nonce-negotiation mechanism, unrelated to the "Token Exchange" grant type this phase's design already covered) — implementing it in `exchange_token_for_runtime()` would have been a real, multi-day feature addition, not a small patch. Confirmed via Okta's own DPoP documentation that this is an app-level toggle, independent of the Token Exchange grant-type setting — most likely enabled by an org-wide default policy or a separate, unrelated configuration action, not anything this session's design work called for. Disabling it was the correct fix rather than building DPoP support, since nothing in this phase's actual requirements needed sender-constrained tokens. |
| 77 | `allowedClients`/`client_id` claim mismatch, again | Removed `allowed_clients` from `RuntimeStack`'s JWT authorizer config entirely (`cdk/app.py` no longer wires `WEB_RUNTIME_OIDC_CLIENT_ID` into it) — validation now relies solely on `allowedAudience`/`allowedScopes`, both confirmed to match real exchanged tokens correctly. `_build_authorizer_configuration()`'s `allowed_clients` parameter is kept (for a caller whose own Okta org does populate a real `client_id` claim) but is no longer wired to anything by default. | A second real live failure, immediately after fixing decision #76: `InvokeAgentRuntime` returned `401 Claim 'client_id' value mismatch with configuration` even though the token exchange itself succeeded. This is the *exact same* real-token-shape mismatch this project already found and fixed once before, during the original (later-reverted) Okta-JWT cutover — decisions #26-35/PLAN.md's "Post-implementation fix: Okta `client_id` claim mismatch" — Okta places the requesting client's identifier in a `cid` claim, not `client_id`, so `allowedClients` (which validates `client_id`) silently rejects every real Okta-issued token from this org. The original fix (drop `allowedClients`, rely on `allowedAudience` instead) generalizes directly to this second, independent JWT authorizer (the Runtime's, for Phase 2) — same root cause, same fix, applied a second time in a different config location. |
| 78 | Dead `runtime.grant_invoke(task_role)` IAM grant, removed (supersedes decision #42's IAM-scope row) | Removed `runtime.grant_invoke(task_role)` from `cdk/stacks/web_stack.py` entirely — the task role no longer has `bedrock-agentcore:InvokeAgentRuntime` permission at all. | Found during a post-Phase-2 doc/code drift audit (2026-08-30): `agent_client.py`'s `stream_agent_events()` is 100% raw-HTTPS/JWT-bearer-token now (decisions #63-64) — confirmed via a repo-wide search that zero `boto3`/`invoke_agent_runtime` calls exist anywhere in `web/`. Since `WebStack` always requires `WEB_RUNTIME_OIDC_*` (switching the Runtime to JWT-only auth per decision #75/AWS's mutual-exclusivity finding), this IAM grant could never actually be exercised by this task role calling the Runtime — it was harmless dead permission, not a live security hole, but removing it keeps the task role's actual grants matching what the code really does, rather than implying a working IAM invocation path that doesn't exist. |
| 79 | `allowedClients`/`client_id` re-enabled (supersedes decision #77's removal) | User reconfigured the Okta client registrations to include a real `client_id` claim on issued tokens. `cdk/app.py` now wires `WEB_RUNTIME_OIDC_CLIENT_ID` into `RuntimeStack`'s `runtime_oidc_allowed_clients` parameter again — the Runtime's JWT authorizer validates `allowedAudience`/`allowedClients`/`allowedScopes` together. | Decision #77's fix (dropping `allowedClients` from validation) was correct *given the token shape at the time* — Okta's `cid`-not-`client_id` claim mismatch made any `allowedClients` config reject every real token. That root cause is now gone: the org's client registrations were updated to actually populate `client_id`. Re-enabling this tightens the authorizer to defense-in-depth (a token must be both correctly audienced/scoped *and* issued to the specific exchange app), rather than accepting any token that merely matches audience/scope. If a future Okta-side change reverts the `client_id` claim (or a different org/app doesn't populate it), every real token will again be rejected with the same `Claim 'client_id' value mismatch` error seen in decision #77 — check the Okta client's claims configuration first if that recurs. |


  │
  └─ Web UI (web/server.py), hosted on ECS Fargate behind a plain
     (non-authenticating) ALB — the server itself runs the OIDC login
     flow against Okta and issues its own KMS-encrypted session cookie
     (or the AgentCore console's test chat, for a Runtime-only sanity
     check — IAM-authorized Runtimes only; confirmed no longer usable
     once this Runtime is switched to JWT, see decision #75's note)
                                       │
                                       │  raw HTTPS POST + JWT bearer
                                       │  token (RFC 8693 token-exchanged
                                       │  from the user's Okta session —
                                       │  Phase 2), streamed as SSE.
                                       │  NOT boto3 — AWS's own docs
                                       │  confirm boto3 cannot invoke a
                                       │  JWT-authorized Runtime at all.
                                       ▼
                          AgentCore Runtime  ── hosts ──▶  Strands Agent (Python, Claude Sonnet via Bedrock)
                                       │                              │
                                       │                              ├─ reads/writes ──▶ AgentCore Memory
                                       │                              │                    (short-term: session state)
                                       │                              │                    (long-term: user preferences)
                                       │                              │
                                       │                              └─ MCP tool calls ──▶ AgentCore Gateway
                                       │                                 (IAM/SigV4 by default via the Runtime's
                                       │                                  own execution role, unchanged by
                                       │                                  Phase 2 — or a per-user, RFC 8693
                                       │                                  On-Behalf-Of-exchanged JWT bearer
                                       │                                  token once the Gateway's own
                                       │                                  authorizer is switched to JWT, §2g)
                                       │                                        ┌─────────────────┼─────────────────┐
                                       │                                        ▼                 ▼                 ▼
                                       │                                 Web Search        Lambda: Weather    Lambda: Places
                                       │                                 (managed           (Open-Meteo API)   (Amazon Location
                                       │                                  connector)                           Service wrapper)
                                       ▼
                       Streamed events: reasoning / text / tool_use /
                       tool_result / done / error — the web UI renders text
                       live and (optionally) all events in a diagnostic panel.
```

The web UI is the only client, using `agent_client.py` (`web/agent_client.py`
as of the CLI removal, decision #48) for building runtime session IDs and
consuming the SSE stream. `actor_id` reaches the invocation payload from
`web/server.py`'s own verified Okta session (decision #58's `sub` claim,
sanitized) — used by `agent.py`'s `get_actor_id()` only as a fallback once
Phase 3 is deployed, since a JWT-authorized Runtime now derives `actor_id`
from its own verified inbound bearer token instead (see §2g decision #80).

## 2g. Phase 3 auth rearchitecture: Gateway JWT authorizer + RFC 8693 On-Behalf-Of token exchange (added 2026-08-30)

Extends the identity-propagation chain one hop further: Phase 2 gave the
Runtime a real, verified per-user JWT for its own inbound auth; this phase
switches the *Gateway's* inbound authorizer to JWT too, and has the
Runtime present a Gateway-audienced, per-user JWT (obtained via RFC 8693
On-Behalf-Of token exchange) instead of signing Gateway calls with its own
IAM execution-role credentials. The design closely follows AWS's own
documented pattern for this exact scenario — see the AWS Security Blog
post "Propagate user authorization context in AI agents with Amazon
Bedrock AgentCore" (19 Aug 2026) — with `AgentCore Identity`
(`GetWorkloadAccessTokenForJWT`/`GetResourceOauth2Token`) doing essentially
all of the actual token-exchange work, rather than this project hand-
rolling an HTTP call to Okta's `/token` endpoint the way Phase 2's
`web/auth.py` does.

Scope for this phase is deliberately narrow: the Gateway does not yet use
the propagated `sub` claim for anything (no per-user tool authorization,
no per-user audit scoping at the target level) — this phase is purely
about establishing the plumbing (a real, verified per-user identity
reaching the Gateway as a bearer token) for a future feature to build on,
not about changing what any tool call is allowed to do today.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 80 | `actor_id` derivation, JWT mode | `get_actor_id()` now reads the caller's verified `sub` claim from the inbound `Authorization` header (via `BedrockAgentCoreContext.get_request_headers()`) when the Runtime's inbound authorizer is JWT, decoding without re-verifying the signature. Falls back to the payload's own `actor_id` field only under IAM inbound auth (no bearer token exists to read a `sub` from in that mode). | Restores decision #31's original "derive server-side from a verified token, never from client input" stance for Memory scoping, which decision #37 had walked back specifically because IAM-only inbound auth left no bearer token to decode. Now that Phase 2 restored a real, cryptographically-verified per-request JWT, this closes that trust gap again. Decoding without re-verifying the signature is safe for the same reason Phase 2's `web/auth.py` decoding is safe (`_sub_from_access_token()`'s own docstring) — anchored differently here: AgentCore Runtime's own JWT authorizer has *already* verified this exact token's signature/issuer/expiry before invoking agent code at all, so by the time `get_actor_id()` runs, the token is already known-good. |
| 81 | Gateway authorizer: IAM (default) vs. JWT | `GatewayStack`'s authorizer switches from `GatewayAuthorizer.using_aws_iam()` to `GatewayAuthorizer.using_custom_jwt()` only when `GATEWAY_OIDC_*` env vars are set — mirrors `RuntimeStack`'s own optional-JWT pattern exactly (same `_build_authorizer_configuration()` shape, same "no Okta prerequisite unless configured" preservation of standalone deployability). | AWS's own docs confirm this Runtime-vs-Gateway JWT/IAM mutual exclusivity is identical for both resource types (`CustomJWTAuthorizerConfiguration` is the same shape for either). Confirmed via the user (this project's design-review answer #17) that no caller other than the Runtime invokes the Gateway directly, so there is no equivalent to decision #75's "AgentCore console test-chat breaks once JWT-only" regression to accept here — nothing else currently depends on the Gateway staying IAM-authorized. |
| 82 | A third, dedicated Okta app for the Gateway OBO exchange | `GATEWAY_OIDC_*` describes a genuinely separate Okta "API Services" app — distinct from both the web-login app (`WEB_OIDC_*`) and Phase 2's Runtime-exchange app (`WEB_RUNTIME_OIDC_*`). Same app-type/config pattern as Phase 2's app (Token Exchange grant type, DPoP disabled, real `client_id` claim on issued tokens) — proactively configured correctly from the start, avoiding a repeat of decisions #76/#79's post-deploy fixes. | Three Okta apps total across this project's full auth rearchitecture, each with a distinct audience: the web-login app (user-facing OIDC), the Runtime-exchange app (`travel-agent-runtime` audience), and the Gateway-exchange app (`travel-agent-gateway` audience) — keeping each hop's trust boundary independently scoped and revocable, rather than reusing one app/audience across multiple, unrelated resource servers. |
| 83 | `actor_token_content: NONE` for the Gateway's OBO exchange | The `CfnOAuth2CredentialProvider`'s `onBehalfOfTokenExchangeConfig` is set to `grantType: TOKEN_EXCHANGE` with `tokenExchangeGrantTypeConfig.actorTokenContent: NONE` — no separate actor token is sent alongside `subject_token` in the exchange request. | Confirmed directly with the user (design-review answer #12): this Okta org's Token Exchange grant does not support or expect an `actor_token` at all — it authenticates purely via client-credential (HTTP Basic) auth plus `subject_token`, the same shape Phase 2's own hand-rolled `exchange_token_for_runtime()` already uses. `M2M`/`AWS_IAM_ID_TOKEN_JWT` (AWS SDK docs' other two `actor_token_content` options) would send an actor token this Okta org doesn't expect, causing an exchange failure. |
| 84 | Runtime execution-role IAM grant: `GetResourceOauth2Token` only, not `GetWorkloadAccessToken*` | `RuntimeStack` grants the Runtime's execution role only `bedrock-agentcore:GetResourceOauth2Token`, scoped to the Gateway's `CfnOAuth2CredentialProvider` ARN. Deliberately does NOT grant `GetWorkloadAccessToken`/`GetWorkloadAccessTokenForJWT`/`GetWorkloadAccessTokenForUserId`. | AWS's own docs are explicit: "Runtime-managed agent identities cannot retrieve workload access tokens directly, preventing token extraction and misuse" — the workload access token this exchange consumes is *automatically* minted and delivered to `agent.py` via the `WorkloadAccessToken` request header (read via `BedrockAgentCoreContext.get_workload_access_token()`), never fetched by a `GetWorkloadAccessToken*` call from agent code itself. Granting those actions would be the same kind of dead/misleading permission decision #78 already found and removed once (`gateway.grant_invoke()`) — confirmed this is a real platform restriction, not just an unused-in-practice grant, before deciding not to add it. |
| 85 | `agent.py`'s Gateway auth switch: raw `mcp.client.streamable_http.streamablehttp_client` with a Bearer header, not a new AWS-specific transport | Once `GATEWAY_OBO_PROVIDER_NAME` is set, `build_mcp_client()` swaps `aws_iam_streamablehttp_client` for the base `mcp` library's own `streamablehttp_client(url=..., headers={"Authorization": f"Bearer {token}"})` — no request signing at all under this mode, since the Gateway is JWT-authorized, not IAM-authorized, once configured this way. | Mirrors Phase 2's own equivalent finding for the Runtime side (`web/agent_client.py`'s rewrite away from `boto3`) — a JWT-authorized AgentCore resource is invoked over plain HTTPS with a bearer token, not through any AWS-signing machinery, regardless of whether the resource is a Runtime or a Gateway. Confirmed the base `mcp.client.streamable_http.streamablehttp_client()` already accepts a `headers` dict, so no new library or custom transport was needed — a much smaller lift than Phase 2's Runtime-side rewrite, which needed a full hand-rolled SSE-parsing HTTPS client since `web/agent_client.py` isn't built on the `mcp` library at all. |
| 86 | Token-exchange mechanics: AgentCore Identity does the work, not hand-rolled HTTP | `build_mcp_client()` calls `bedrock_agentcore.identity.auth.IdentityClient.get_token(auth_flow="ON_BEHALF_OF_TOKEN_EXCHANGE")`, which internally calls `GetResourceOauth2Token` — `agent.py` never makes an HTTP request to Okta directly, and never touches the Okta client secret (held in a dedicated Secrets Manager secret, read only by AgentCore Identity server-side). | A materially different implementation shape from Phase 2's `web/auth.py::exchange_token_for_runtime()`, which is a hand-rolled `requests.post()` against Okta's `/token` endpoint with the client secret held directly in the web server's own process. This phase's approach is the AWS-native path (confirmed against `docs.aws.amazon.com/bedrock-agentcore` and a directly-matching internal precedent used for the same "Runtime calling a JWT-authorized Gateway via OBO" scenario) — chosen because AgentCore Identity already exists specifically for this, and reimplementing Okta's token-exchange call a second time inside `agent.py` would duplicate real security-sensitive logic (client-secret handling, response parsing, error handling) that AgentCore Identity already handles correctly. |
| 87 | Scope: plumbing only, no `sub`-based authorization yet | The Gateway's tools (Web Search, weather, places) do not read or act on the propagated `sub` claim in any way — this phase changes only how the *inbound call itself* is authenticated, not what any tool call is allowed to do. | Confirmed directly with the user (design-review answers #1/#2): this phase exists purely to establish real per-user identity propagation as infrastructure for a *future*, not-yet-designed feature (e.g. per-user tool authorization or audit scoping) — deliberately not conflating "can we prove who the caller is" with "should we use that to change behavior," which would have meaningfully expanded this phase's scope beyond what was asked for. |

### Live deployment findings (added 2026-08-31, after the design above was implemented and deployed)

Six distinct, real problems surfaced only once this phase was actually
deployed and exercised live — none of them visible from `cdk synth`/unit
tests alone. Recorded here because each is a genuinely reusable
operational lesson for AgentCore Gateway/Runtime work, not specific to
this project.

| # | Finding | What actually happened | Fix / takeaway |
|---|---------|------------------------|-----------------|
| 88 | `AWS::BedrockAgentCore::Gateway` can't change `authorizerType` in place | AWS's `UpdateGateway` API hard-rejects an authorizer-type change on an existing Gateway (`"Authorizer type cannot be updated for an existing gateway"`) — confirmed via a direct API call, not a CDK/CloudFormation limitation. CloudFormation's own resource handlers for both `Gateway` and any `AWS::Logs::DeliverySource` whose `ResourceArn` points at that Gateway don't fall back to replacement automatically either — they attempt an in-place `Modify` and fail. | Switching IAM→JWT required forcing full CDK-level replacement by renaming the Gateway's own L2 construct ID (`"TravelAgentGateway"` → `"TravelAgentGatewayV2"`, kept permanently — renaming again would trigger this whole cycle a second time for no benefit) *and* renaming every dependent construct whose properties derive from the Gateway's ARN/ID (here: `GatewayLogsDeliverySource`/`GatewayTracesDeliverySource`, renamed to `...V2` with new `name=` literals too, since delivery-source names must be account-unique). The 3 `GatewayTarget`s and the `CfnOAuth2CredentialProvider` did *not* need manual renaming — CDK/CloudFormation correctly replaces those automatically once the Gateway itself is a new logical resource. |
| 89 | CFN doesn't order Delete-before-Add for same-named unrelated resources | Even after the construct-ID rename produced a clean `Add`+`Delete` changeset, the deploy failed with `"A gateway with name '...' already exists"` — CloudFormation creates the new (renamed) resource before deleting the old one when they're two different logical resources, even though they share the same literal `name=` property. | No CDK-level fix exists for this ordering; resolved by manually deleting the old Gateway's targets then the Gateway itself via direct `bedrock-agentcore-control` API calls before retrying the deploy. Worth knowing before attempting a similar Gateway-identity-changing deploy: expect to manually clear the old physical resource's name collision, not rely on CloudFormation to order it correctly. |
| 90 | `CfnOAuth2CredentialProvider`'s `clientSecretConfig` (Secrets-Manager reference) is rejected for `CLIENT_SECRET_BASIC`/`CLIENT_SECRET_POST` | The live `CreateOauth2CredentialProvider` API call rejected a `clientSecretConfig`/`SecretReferenceProperty` (Secrets Manager reference) with `"clientSecret is required for CLIENT_SECRET_BASIC and CLIENT_SECRET_POST authentication methods"` — even with `clientAuthenticationMethod` set explicitly. This directly contradicts the assumption baked into the original synth-only-verified code (see PLAN.md Phase 14's superseded "What was built" note, which described a `SecretReferenceProperty`-based approach). | Switched to `client_secret=client_secret` as a plain literal string (`SecretValue.unsafe_plain_text()`'s value passed directly), matching how `WebStack`'s own `OidcClientSecret`/`RuntimeOidcClientSecret` already handle secrets for this project. Confirmed this is a live-API-only failure mode — `cdk synth` alone can't catch it, since synth only validates the CloudFormation template shape, not runtime API acceptance of specific property combinations. |
| 91 | `GetResourceOauth2Token` requires IAM grants on multiple resource families simultaneously | The original IAM grant (decision #84) — `GetResourceOauth2Token` scoped only to the credential-provider ARN — produced three *sequential* `AccessDeniedException`s once actually exercised live, each naming a different resource: first the Runtime's own `workload-identity-directory/.../workload-identity/*`, then the account's `token-vault/default` resource itself (distinct from, and required in addition to, the credential-provider-*within*-the-vault ARN). Granting one cleared that specific check and immediately exposed the next. | Final grant needs all of: the credential-provider ARN, `token-vault/default`, `workload-identity-directory/default`, and `workload-identity-directory/default/workload-identity/{runtime_name}-*` (a **wildcard prefix**, not the resolved `agent_runtime_id` — using the exact resolved ID via `self.runtime.agent_runtime_id` creates a genuine circular CloudFormation dependency, since that's a `Fn::GetAtt` on the very Runtime resource whose own execution-role policy is being defined). Also needs `secretsmanager:GetSecretValue` scoped to `bedrock-agentcore-identity!default/oauth2/{provider_name}-*` — AgentCore Identity reads the credential provider's client secret from a Secrets Manager secret it creates and names itself under a fixed prefix, which the exchange call needs read access to. Multiple internal reference implementations (independently discovered via code search) confirm this exact multi-resource-family requirement is a known, recurring gotcha for `GetResourceOauth2Token`, not something specific to this deployment. |
| 92 | AgentCore Identity's `TOKEN_EXCHANGE` grant defaults to a `subject_token_type` this Okta org rejects | AgentCore Identity's `TOKEN_EXCHANGE` grant mode unconditionally sends `subject_token_type=urn:ietf:params:oauth:token-type:jwt` to the IdP — confirmed live via Okta's own System Log (`invalid_subject_token_type`) that this org's custom authorization server only accepts `urn:ietf:params:oauth:token-type:access_token` for the Token Exchange grant (matching Okta's own documented examples, and explicitly called out as an Okta-specific pitfall in AWS's "Implement on-behalf-of token exchange for multi-tenant agents with Amazon Bedrock AgentCore Gateway" blog post, 13 Jul 2026). There is no CDK/`CfnOAuth2CredentialProvider`-level field for this — `TokenExchangeGrantTypeConfigProperty` only exposes `ActorTokenContent`/`ActorTokenScopes`, confirmed against the full CFN property schema. | Fixed per-call, not per-provider: `agent.py`'s `IdentityClient.get_token()` call passes `custom_parameters={"subject_token_type": "urn:ietf:params:oauth:token-type:access_token"}`, which maps directly onto `GetResourceOauth2Token`'s own `customParameters` request field (confirmed against the installed `bedrock_agentcore` SDK source: `req["customParameters"] = custom_parameters`). Worth checking for any IdP integration using AgentCore Identity's `TOKEN_EXCHANGE` mode — the AWS-side default may not match what the specific authorization server expects. |
| 93 | A stale local `__pycache__` can silently ship stale bytecode through every future deploy | `agent/__pycache__/agent.cpython-312.pyc`, left on disk from local `pytest`/`python` runs, was blindly copied into every deployed AgentCore Runtime code asset by the bundling command's `cp -r . /asset-output` step. Once present, every subsequent code change to `agent.py` appeared to have zero effect live — new logging lines, new logic, nothing showed up — even though the deployed `.py` source file was independently confirmed byte-identical to the local source on every single deploy. Root-caused only by downloading the actual deployed S3 asset zip and finding the stale `.pyc` alongside a byte-identical `.py`; Python was loading the cached bytecode instead of recompiling. | `cdk/stacks/runtime_stack.py`'s `BundlingOptions.command` now runs `find /asset-output -depth -type d -name __pycache__ -exec rm -rf {} +` after the `cp -r`, stripping any `__pycache__` directories unconditionally. Confirmed effective: the next asset dropped from 69.9 MiB to 49.9 MiB (accumulated `.pyc`/`__pycache__` bloat across many previous local runs, not just this one file) and contained zero `__pycache__` entries. General lesson: for any `AgentRuntimeArtifact.from_code_asset()`/similar "just copy the source directory" bundling command, always strip bytecode caches explicitly — don't assume a plain recursive copy is safe just because the visible source files match. |
| 94 | AgentCore Runtime pins each session's code to whatever was deployed when that session was first created | Per AWS's own troubleshooting doc (`runtime-troubleshooting.html`, "My code changes aren't reflected in existing sessions"): "Each microVM session is created with the code assets that were deployed at the time of session creation. Once a session is established, it continues using that version of the code until the session terminates, even when code assets are updated." This produced a long, confusing debugging detour during this phase's live verification: repeated `UpdateAgentRuntime` deploys appeared to have no effect because every test reused the same existing web-UI conversation (and thus the same already-warm microVM/session), never actually exercising the newly-deployed code. | Always start a **new conversation** (new session ID) in the web UI after any Runtime code deploy before concluding a fix didn't work — this project's web UI's own "New conversation" button exists for exactly this. Confirmed via CloudWatch log stream names (`.../[runtime-logs-web-session___<id>]...`) that reused sessions kept emitting log lines from stale code (old line numbers, missing new log statements) for over 20 minutes and across 3 separate `UpdateAgentRuntime` deploys, purely because the same session ID was reused throughout. |
| 95 | `RequestHeaderConfiguration`/`allowlisted_headers` must be set explicitly for the inbound `Authorization` header to ever reach agent code — even with `CUSTOM_JWT` auth configured | Decision #34 (Phase 2, above) already correctly *identified* this requirement in the abstract ("does not happen automatically from JWT authorizer configuration alone"), but Phase 3's actual `RuntimeStack` code never implemented it — `authorizer_configuration` was set, but `request_header_configuration` was not. Confirmed live via a diagnostic log dump of `BedrockAgentCoreContext.get_request_headers().keys()`: the inbound request carried only `['baggage', 'workloadaccesstoken']`, never `Authorization`, despite `web/agent_client.py` correctly sending `Authorization: Bearer <token>` on every call and AWS's own `runtime/app.py` SDK code unconditionally special-casing `Authorization` for forwarding *if the control plane had already decided to hand it to the container at all*. This silently broke `_sub_from_authorization_header()` (always returned `None`), which in turn meant the Gateway-OBO-token cache (added this phase, see #96) never got a real cache key and re-exchanged on every single turn — the original symptom that triggered this entire investigation. | `runtime_stack.py`'s `agentcore.Runtime(...)` now also sets `request_header_configuration=agentcore.RequestHeaderConfiguration(allowlisted_headers=["Authorization"])`, gated on the same `runtime_oidc_discovery_url` condition as `authorizer_configuration` (no `Authorization` header exists to allowlist under the IAM/SigV4 default). Confirmed live post-fix: header keys became `['Authorization', 'baggage', 'workloadaccesstoken']`, `_sub_from_authorization_header()` correctly resolved `sub`, and the OBO cache immediately started hitting on subsequent turns. **Lesson: a documented design decision existing in DESIGN.md is not the same as it being implemented** — re-verify decisions like #34 are actually reflected in the current code for every stack/resource they apply to, not just the one they were originally written against. |
| 96 | Gateway-OBO-token cache: added to reduce Okta round trips, correctness depends on cache-key scoping and container-lifecycle awareness | `agent/agent.py`'s `_GATEWAY_OBO_TOKEN_CACHE` (module-level dict, `threading.Lock`-guarded, keyed by the inbound JWT's `sub`) avoids re-exchanging on every conversation turn — added specifically because AgentCore Identity's own server-side caching for the OBO-brokered token is confirmed (per direct Slack confirmation from the AgentCore Identity SDM) still in progress as of this writing, expected by end of September 2026. TTL is read from the exchanged token's own `exp` claim (with a 60s refresh skew), not a guessed constant. | Correctness (not just efficiency) requires keying by `sub`, not caching one global token — a single container can serve different end users across requests, and any single-key cache would hand one user's delegated Gateway token to a different user's request. Cache lifetime is bounded by whichever comes first: the token's own ~1-hour expiry, the session's `idleRuntimeSessionTimeout` (900s/15min default — an idle gap longer than this recycles the microVM and wipes the in-process cache), or `maxLifetime` (28800s/8h default, a hard ceiling regardless of activity). In practice this means one Okta exchange roughly per hour of continuous use, or once per fresh/resumed-after-15-min-idle conversation — a large reduction from the pre-fix "every single turn," but not a guaranteed fixed cadence. Extending `idleRuntimeSessionTimeout` would reduce cold-start/re-exchange frequency for users with natural mid-conversation pauses, at the cost of billing peak session memory for longer during those idle gaps (AWS's AgentCore Runtime pricing page: CPU during idle/I-O-wait is free, but peak memory consumed remains billable for the whole session span including idle time) — not implemented in this phase; left as a tunable if user pause patterns in practice warrant it. |



## 2h. Observability pass: logging, tracing, log retention across all 5 stacks (added 2026-08-31)

Before this phase, observability was uneven across the project: `GatewayStack`
had application logs + X-Ray traces (§2a decision #36) and `RuntimeStack` had
`tracing_enabled=True` (§2g), but the two Lambda tool handlers emitted
nothing beyond Lambda's own automatic invocation/error/duration lines, the
Web stack's ECS container had no tracing and no explicit log retention, and
every log group in the project except Gateway's had no retention set at all
(CloudWatch's default is to keep vended logs forever, silently accumulating
cost). This phase closes those gaps uniformly across all 5 stacks — Tools,
Gateway, Memory, Runtime, Web — rather than only the ones that already had
partial coverage.

Deliberately scoped to **logs, traces, and log retention only**. Custom
application metrics (e.g. an explicit "OBO cache hit rate" metric), CloudWatch
Alarms, and a CloudWatch Dashboard were all explicitly discussed and deferred
— see decisions #101-103 below for why each was cut from this pass rather
than silently omitted.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 97 | Lambda handlers get real structured logging, not just infra-level log retention | `lambdas/weather/handler.py` and `lambdas/places/handler.py` both gained `logger.info()`/`.warning()`/`.error()`/`.exception()` calls at the request, upstream-call (Open-Meteo HTTP / `geo-places:SearchText`), and response boundaries — matching `agent/agent.py`'s existing `logging.basicConfig(level=logging.INFO)` style. Purely additive: no return values or control-flow branches changed, confirmed by all 33 existing Lambda tests passing unmodified. | Prior to this, both Lambdas were completely silent beyond Lambda's own automatic invocation/duration/error lines — a bad Open-Meteo response, a geocoding miss, or an empty/erroring `geo-places` result was indistinguishable from any other failure without re-deriving it from the Gateway's own already-opaque error message. This was the single biggest blind spot in the whole stack; every other component already had at least some request-level logging. |
| 98 | Classic X-Ray SDK (the originally planned approach for Web) doesn't support FastAPI — switched to the OpenTelemetry-based ADOT collector for Web only | AWS's own X-Ray SDK for Python docs (`xray-sdk-python-middleware.html`) confirm the SDK's automatic incoming-request middleware supports only Django, Flask, and Bottle — not FastAPI/ASGI, which `web/server.py` is built on. AWS's docs also carry a standing notice that the classic X-Ray SDK/daemon entered maintenance mode (25 Feb 2026) with an explicit recommendation to migrate to OpenTelemetry. `web/server.py` is instrumented with `opentelemetry-instrumentation-fastapi`'s `FastAPIInstrumentor`, exporting via OTLP/gRPC to an ADOT collector sidecar (`public.ecr.aws/aws-observability/aws-otel-collector:v0.30.0`, the exact image/config AWS's own ECS docs document for "trace collection") added to the same ECS task — the two containers share the task's `awsvpc` network namespace, so `localhost:4317` in `WebContainer` reaches the sidecar directly. The ADOT collector forwards to X-Ray using the task role's `AWSXRayDaemonWriteAccess` managed policy — the same permission set the classic X-Ray daemon itself would need. | This is a genuine, user-confirmed deviation from the plan as originally agreed ("X-Ray daemon sidecar" for all of Tools/Gateway/Runtime/Web) — flagged mid-implementation once the FastAPI-support gap was discovered, rather than silently forcing an unsupported integration (hand-rolled ASGI middleware against a library not designed for it) or quietly switching approaches without asking. Lambdas keep `Tracing.ACTIVE` (a Lambda platform feature, unrelated to this SDK choice) and Gateway/Runtime keep their existing X-Ray-native tracing — only Web's collection mechanism differs. Both ultimately land in the same X-Ray console/trace map. |
| 99 | Uniform `RetentionDays.ONE_MONTH` on every log group in the project | Explicit log groups (with retention) were added for: both Lambdas (`WeatherFunctionLogGroup`/`PlacesFunctionLogGroup`, previously Lambda's un-retained default), the Web ECS container (`WebContainerLogGroup`, previously un-retained via `ecs.LogDrivers.aws_logs()` with no `log_group=` override), the new ADOT sidecar (`AdotCollectorLogGroup`), and the new ALB access-logs destination (`AlbAccessLogGroup`, see decision #100). The Runtime's own log group is trickier — see decision #100a. Gateway's log group already had this retention from §2a decision #36 and is unchanged. | 30 days matches the precedent decision #36 already established for Gateway rather than introducing a second convention. Before this phase, every log group *except* Gateway's had no explicit retention at all — CloudWatch's default for vended/awslogs-driver log groups is to never expire, so this was a real, silently-compounding cost risk (not just an observability gap) across 4 of the 5 stacks. |
| 100 | ALB access logs delivered to CloudWatch Logs directly, not S3 | `web_stack.py` adds an `AWS::Logs::DeliverySource`/`DeliveryDestination`/`Delivery` triple (`LogType: ALB_ACCESS_LOGS`, `DeliveryDestinationType: CWL`) targeting a new, retained log group — CloudWatch's newer direct-to-CWL vended-logs integration for ALB, not the older `access_logs.s3.*` load balancer attribute. Mirrors the exact pattern `gateway_stack.py` already established for the Gateway's own application logs/traces (§2a decision #36) — no new delivery mechanism introduced for this project. | Chosen specifically so ALB request-level logs land alongside every other component's logs in CloudWatch, rather than introducing a new S3 bucket + lifecycle policy just for this one log type — this is a genuinely newer AWS capability (not yet exposed as a CDK L2 construct; modeled directly via the L1 `Cfn*` resources, same as Gateway's own observability code) confirmed against a real, working CloudFormation example before use, since AWS's own documentation pages for this feature don't spell out the exact `LogType` literal string (`ALB_ACCESS_LOGS`) directly. |
| 100a | Runtime's own auto-created log group required a `LogRetention` custom resource, not a direct property | `runtime_stack.py` adds `logs.LogRetention(log_group_name=f"/aws/bedrock-agentcore/runtimes/{self.runtime.agent_runtime_id}-DEFAULT", retention=ONE_MONTH)`. CDK's own `aws_bedrockagentcore` module docstring confirms AgentCore creates this log group itself, lazily, on the Runtime's first invocation — not at CDK deploy time — so there is no CDK-level property on the `Runtime` construct to set retention directly; `LogRetention` (a CDK-managed custom resource backed by an auto-generated Lambda) is the documented workaround for exactly this situation. | This is a distinct implementation shape from every other log group in this phase (which are all plain `logs.LogGroup(retention=...)` at deploy time) — worth recording so a future reader isn't confused by the inconsistency, or tempted to "simplify" it into a direct property that doesn't exist. The log group name is built from `self.runtime.agent_runtime_id` (a token resolved at deploy time), not the static `RUNTIME_NAME` constant, matching AWS's documented `{agentRuntimeId}-DEFAULT` naming convention exactly. |
| 101 | No custom application metrics this pass | Considered and explicitly rejected for this pass: an OBO-cache hit-rate metric, per-Lambda upstream-call latency separated from total duration, and Gateway tool-call outcome by target — all real gaps AWS's free per-service metrics can't answer on their own. Deferred rather than built. | Every AWS service in this project already emits its own automatic CloudWatch metrics (Lambda: Invocations/Errors/Duration/Throttles; ECS: CPU/MemoryUtilization; ALB: RequestCount/TargetResponseTime/5XX; AgentCore Runtime/Gateway: their own namespace) with zero code required — that's judged sufficient for this pass's stated goal (logs/traces to aid manual troubleshooting), and custom metrics require genuinely new code (EMF or `PutMetricData`) that's a meaningfully different scope than "turn on what already exists." Revisit if a specific metric proves necessary once the new logs/traces are actually used in practice. |
| 102 | No CloudWatch Alarms this pass | Considered and explicitly rejected: alarms on Lambda errors/throttles, ECS task failures, ALB 5xx rate, Gateway/Runtime invocation errors. | This phase's stated goal was diagnostic visibility ("aid in troubleshooting" — reactive, look-when-something-feels-off), not proactive notification, which is a materially different feature requiring a real notification target (SNS → email/Slack) and threshold judgment calls that are easy to get wrong without any historical baseline data — and there is none yet, since nothing was instrumented before this phase. This project's README already frames the Web deployment as sized for "a small, known group of users," which further argues against proactive paging over manual review. Natural fast-follow once the new logs/traces/metrics have been used in practice and real thresholds can be set with confidence. |
| 103 | No CloudWatch Dashboard this pass | Considered and explicitly rejected: a single aggregated view across all 5 components. | A dashboard is only as useful as the data feeding it, and useful widget choices are much easier to make correctly *after* using the new logs/traces to debug something real, rather than guessing at a layout before any of this phase's new data has actually been used in practice. Same reasoning as deferring alarms (decision #102) — establish the underlying data first, build visualization/alerting on top of a working foundation second. |
| 104 | 100% X-Ray sampling via a single account/region-wide `CfnSamplingRule`, not X-Ray's built-in default | `tools_stack.py` adds one `xray.CfnSamplingRule` (`priority=1`, `fixed_rate=1.0`, `reservoir_size=1`, every match field — `service_name`/`service_type`/`host`/`http_method`/`url_path`/`resource_arn` — set to `"*"`), overriding X-Ray's own built-in default rule (fixed `priority: 9999`, 1 req/sec + 5% of the rest) for every traced resource in the account: both Lambdas, the Gateway, the Runtime, and Web's ADOT-sourced spans alike. Placed in `ToolsStack` specifically because X-Ray sampling rules have no per-resource `resource_arn` scoping mechanism that would make them naturally belong to any one stack — `ToolsStack` deploys first and has no stronger or weaker claim to owning this than any other stack. Rule name kept to 27 characters (`travel-agent-full-sampling`) after `cdk synth --all` caught a real CFN validation warning at 35 characters, exceeding X-Ray's 32-character limit. | Given this project's actual traffic profile (a small, known group of users, not a high-volume service — see README), X-Ray's default 5%-of-overflow sampling could easily mean the one request being actively debugged never gets sampled at all. 100% sampling costs negligibly more in absolute terms at this traffic volume and directly serves this phase's "maximum observability to aid troubleshooting" goal — partial sampling would work directly against that stated goal. |
| 105 | ADOT collector config file: `ecs-default-config.yaml`, not `otel-instance-metrics-config.yaml` — found via a live deployment failure | The first live deploy of this phase crash-looped: `AdotCollector` exited 1 immediately with `Error: cannot start pipelines: failed to initialize NodeCapacity: lstat /rootfs/proc: no such file or directory`, which cascaded into `WebContainer` being killed (exit 137) once its `ContainerDependencyCondition.START` dependency never became healthy, tripping the ECS deployment circuit breaker and triggering an automatic rollback (`web_stack.py`'s own `circuit_breaker=ecs.DeploymentCircuitBreaker(rollback=True)` — confirmed live it worked exactly as designed, with zero end-user-visible downtime; `/api/config` returned `200` throughout). Root cause: `/etc/ecs/otel-instance-metrics-config.yaml` (the config AWS's generic ECS "trace collection" doc example uses — the one originally copied for decision #98) bundles a host-metrics receiver expecting a real EC2 host filesystem mount, which Fargate does not provide. AWS's own ADOT-on-ECS docs (`aws-otel.github.io/docs/setup/ecs/task-definition-for-ecs-fargate`) are explicit that Fargate tasks need one of two different, Fargate-safe bundled configs instead; switched to `/etc/ecs/ecs-default-config.yaml` (OTLP/StatsD/X-Ray SDK traces, no host metrics — confirmed via the ADOT collector's own GitHub-hosted `ecs-cloudwatch-xray.yaml` reference config, which uses only `env`/`ecs`/`ec2` resource detectors, no filesystem access). | A cautionary tale specifically about copying an AWS doc example without checking whether it's launch-type-specific: the generic ECS "how to add an ADOT sidecar" doc page (used for decision #98) doesn't mention EC2-vs-Fargate at all, but the dedicated Fargate setup tutorial does, with two different bundled configs neither matching the generic page's example. `cdk synth`/`cdk diff` cannot catch this class of failure — the task definition is syntactically and structurally valid either way; the failure only manifests once the container actually tries to start against real Fargate infrastructure. Re-verify config-file-driven container behavior (not just JSON/YAML shape) against the specific launch type actually in use, not just the first matching doc page found. |

## 2i. Gateway-routed inference for centralized governance (added 2026-09-01)

Motivation: centralized governance/guardrails across all model calls, and
the ability to enforce per-individual or per-group token limits — neither
of which is possible while the agent calls `bedrock-runtime` directly.
AgentCore Gateway's `inference` target type (a fourth target, alongside
the existing three tool targets — see §2a) routes model calls through the
Gateway instead, where AWS's own rate-limiting/RBAC layer (`Policy in
AgentCore`, `CreateGatewayRateLimit`) can dimension on the caller's real
JWT claims. This pass wires the routing path only — the actual rate
limits/policy configuration is a deliberate fast-follow (decision #109
below), once a real deployed token's claims can be inspected to design
around, rather than guessed at blind.

Final shipped shape: a `bedrock-mantle` connector target
(`connectorId="bedrock-mantle"`), NOT a hand-rolled `provider`-type
target against `bedrock-runtime`, which was the originally-implemented
approach (decision #106 below records why it was abandoned, after two
successive live-deploy failures — kept here rather than silently
deleted, per this project's own convention of recording real dead ends,
not just the final answer). All live findings in this section came from
actual `cdk deploy` attempts and real API error messages inspected
directly (a raw HTTP debug probe in `agent.py`, removed before this
landed) — none were resolved by documentation alone.

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 106 | Abandoned a hand-rolled `provider`-type target against `bedrock-runtime`; switched to the built-in `bedrock-mantle` connector | The original plan (endpoint=`bedrock-runtime`, explicit `operations`/`provider_path`) failed live twice in a row: (1) a Coral `com.amazon.coral.service#UnknownOperationException` (HTTP 200, not an error status) — `bedrock-runtime`'s real Anthropic-native path is `/anthropic/v1/messages`, not `/v1/messages` (`bedrock-mantle`'s own convention, which every documented provider-target example happens to use, masking the distinction); (2) once that path was corrected, a real Anthropic `401` ("Credential should be scoped to correct service: 'bedrock'") — `GATEWAY_IAM_ROLE`'s generic SigV4 signing has no way to know it should sign as service `bedrock` for that specific route, and every one of AWS's own documented examples for this exact route use a Bedrock API key, not a hand-signed SigV4 request through a generic HTTP proxy. Rather than provisioning a new Bedrock API key + AgentCore Identity `API_KEY` credential provider (a new secret/credential to manage, changing this feature's security posture), switched to `connector_id="bedrock-mantle"` — AWS's own purpose-built integration handles endpoint/path/auth wiring internally. | `bedrock-runtime` is still AWS's own recommended endpoint in the abstract, and the earlier live test correctly showed `bedrock-mantle` failing at *target creation* (missing `ListModels` IAM, decision #106's original framing) while a `bedrock-runtime` provider target reached `READY` — but "reaches READY" only proved the target's *config* was accepted, not that a real inference call would actually work end-to-end. That gap is exactly why this pass could not have been verified as complete until a real, live user turn was tested — config-time success and runtime success are genuinely different questions for this feature, a distinction this project's own decision #105 (the ADOT Fargate bug) had already surfaced once before in a different context. |
| 107 | `bedrock-mantle` connector target needs its own distinct IAM grant: `bedrock-mantle:ListModels`/`CreateInference` (scoped to `arn:aws:bedrock-mantle:{region}:{account}:project/default`) and `bedrock-mantle:CallWithBearerToken` (no resource-level scoping available) | Confirmed against AWS's own `AmazonBedrockMantleInferenceAccess` managed policy — granted here as the same individually-listed actions (not that policy's `Get*`/`List*` wildcards), matching this project's narrow-scoping precedent. Kept alongside the original `bedrock:InvokeModel`/`InvokeModelWithResponseStream` grant (scoped to the `us.`-prefixed inference-profile ARN `MODEL_ID` uses for the *direct* `BedrockModel`/`Converse` path) — left in place even though it's not confirmed to be exercised by the connector specifically, since it's harmless and narrowly scoped, and removing it without proof it's truly unused risked an unnecessary third round of live-deploy trial and error. | The connector target's own creation synchronously calls `bedrock-mantle:ListModels` to discover available models — a real, functional check, not just config acceptance (unlike the abandoned provider-target's `READY` status, which never actually exercised this call). |
| 108 | IAM grant explicitly ordered before target creation via `node.add_dependency(...policy_dependable)`, not incidental declaration order | Found live: a `CREATE_FAILED`/clean-rollback with `403 ... no identity-based policy allows the bedrock-mantle:ListModels action` when the IAM statement and the target were both new resources in the same CloudFormation changeset with no explicit dependency — CloudFormation doesn't infer that the target's own creation-time API call needs the IAM policy to have propagated first. Depends on the `AddToPrincipalPolicyResult.policy_dependable` CDK returns from the grant call, not `self.gateway.role` itself (the role resource existing doesn't guarantee its policy attachment finished). | A second real, live-only failure mode (config-valid, IAM-eventually-correct, but wrong within-changeset ordering) that neither `cdk synth` nor a template diff can catch — the same class of lesson as decision #105 (re-verify runtime behavior, not just structural/IAM correctness, against what actually happens at deploy time). |
| 109 | Target's CDK logical ID and `name=` both suffixed `...V2`/`...-v2` on the connector switch, forcing full resource replacement | AWS's `UpdateGatewayTarget` API rejects an in-place `provider`→`connector` target-configuration-type change ("Target configuration cannot be updated from provider to connector") — confirmed live, with a clean `UPDATE_ROLLBACK_COMPLETE` and no partial state. Same replace-via-logical-ID-change pattern this file already uses for the Gateway resource itself and its log-delivery sources, for the analogous `authorizerType` immutability constraint. | A third distinct class of AWS-API immutability constraint discovered live in this one feature (alongside the Gateway's own `authorizerType` and the target-creation IAM-ordering issue above) — worth naming explicitly so a future reader recognizes the pattern (check for in-place-update rejections before assuming a straightforward property change is safe) rather than being surprised again. |
| 110 | Gateway-routed inference calls use the bare foundation-model ID (`anthropic.claude-sonnet-5`), not `MODEL_ID`'s own `us.`-prefixed cross-region-inference-profile form — a new `GATEWAY_INFERENCE_MODEL_ID` constant in `agent.py`, not a change to `MODEL_ID` itself | Found live: routing `us.anthropic.claude-sonnet-5` through the `bedrock-mantle` connector returned a real Anthropic `404` ("Model 'us.anthropic.claude-sonnet-5' not found on any target"). Confirmed via `aws bedrock list-foundation-models` that the canonical model ID is the bare `anthropic.claude-sonnet-5` (`inferenceTypesSupported: INFERENCE_PROFILE` — the `us.` prefix is a `bedrock-runtime`/`Converse`-specific inference-profile wrapper concept, not something `bedrock-mantle`'s own model routing resolves). `GATEWAY_INFERENCE_MODEL_ID` strips a literal `"us."` prefix if present — not a general regex/split on `MODEL_ID`, to avoid silently mis-stripping a differently-shaped future `MODEL_ID`. | The direct `BedrockModel`/`Converse` path and the Gateway-routed `bedrock-mantle` path genuinely need two different spellings of "the same model" — a real, AWS-side inconsistency between how the two Bedrock surfaces resolve model identity, not a bug in either this project's code or its understanding of one API considered in isolation. Verified end-to-end after this fix: a real turn showed `gen_ai.request.model: "anthropic.claude-sonnet-5"`, real non-zero token usage (`input_tokens: 4249`, `output_tokens: 352`), and a genuine multi-second `chat` span duration in X-Ray — the first fully successful live Gateway-routed inference call, confirming the whole feature actually works end-to-end. |
| 111 | Only the Anthropic Messages API path (`/v1/messages`) is used — not the OpenAI-shaped `/v1/chat/completions`/`/v1/responses` paths the connector also supports | Read Strands' actual model-provider source (`strands/models/openai.py`, `openai_responses.py`, `anthropic.py`) before deciding. Both OpenAI-shaped providers log a warning and **drop `reasoningContent` blocks entirely on multi-turn re-send** ("reasoningContent is not supported in multi-turn conversations with the Chat Completions API" / "...Responses API"). Only the Anthropic provider round-trips `reasoningContent` correctly on input, reformatting it back into a proper `{"type": "thinking", ...}` block. Chose `strands.models.anthropic.AnthropicModel` accordingly. | This agent already relies on Claude's adaptive extended-thinking output surfaced through Bedrock's native `Converse` API specifically to drive the diagnostic panel's "reasoning" stream event. Since this agent's `SummarizingConversationManager` re-sends prior assistant messages (including any `reasoningContent` blocks) as conversation history on every subsequent turn, routing through an OpenAI-shaped path would have silently lost the model's own prior reasoning context starting at turn 2 of every conversation. Discovered and resolved *before* writing any code, not after a live regression — the one design decision in this feature that held up unchanged through every later live-deploy correction. |
| 112 | Rate limiting and RBAC policy (`CreateGatewayRateLimit`, `Policy in AgentCore`) deliberately deferred to a fast-follow, not part of this pass | This pass wires the inference target, the model-provider switch, and the new IAM statements only. No `CreateGatewayRateLimit` call, no dimension-key/entry design, no Cedar policy. | `CreateGatewayRateLimit`'s dimension keys (`$.context.jwt.<claim>`, e.g. `role`/`sub`) require knowing what claims this project's Okta org's issued tokens actually carry on the Gateway-audienced OBO JWT — genuinely unverified without a live, real user session (this same project has twice before discovered an Okta claim didn't carry the value assumed, decisions #77/#79's `allowedClients`/`client_id` mismatch). Land the base routing path (now genuinely verified working end-to-end, not just deployed), inspect a real decoded token, then design limits around what's actually there. |
| 113 | Opt-in via a single `GATEWAY_INFERENCE_URL` env var, defaulting to unset (old `BedrockModel`/direct-`bedrock-runtime` behavior unchanged) | `RuntimeStack` always wires `GATEWAY_INFERENCE_URL` (the inference target always exists), but `agent.py`'s `build_model()` only routes through it when non-empty. | Matches this project's consistent pattern for every optional capability added so far — additive and env-var-gated, never a forced cutover. In practice this also meant every failed attempt during this feature's live debugging happened with the flag *on* in this test environment (explicitly acceptable here per direct instruction — this environment tolerates live breakage in service of reaching a genuinely verified fix, rather than reverting at the first sign of trouble), while the flag's off-by-default design still means a fresh deploy elsewhere starts from known-good behavior. |
| 114 | `model_id` threaded through `cdk/app.py` as a single source of truth, replacing the prior two independently-hardcoded copies | `cdk/app.py` now reads `MODEL_ID` once and passes it into both `GatewayStack` (the inference target's IAM ARN scoping) and `RuntimeStack` (the Runtime's own `MODEL_ID` env var, previously `runtime_stack.py`'s own `DEFAULT_MODEL_ID` module constant, now removed). | The README already carried a self-acknowledged warning that `agent/agent.py`'s `MODEL_ID` default and `runtime_stack.py`'s `DEFAULT_MODEL_ID` constant "must stay in sync." Threading it through `app.py` fixes that existing duplication risk as a side effect of this change. |
| 115 | Per-user token-per-minute rate limit on the inference target, the fast-follow decision #112 deferred | `CfnGatewayRateLimit` (`rate_limit_id="inference-tpm-per-user"`), `dimensionKeys=["qualifiedModelId", "$.context.jwt.sub"]`, a single entry (`qualifiedModelId`: the bare model ID, `$.context.jwt.sub`: `"*"`), `tokens: [{rate: 50000, period: "minute"}]`. | Landed once the base routing path was genuinely verified working end-to-end (decision #110's live confirmation) — exactly the trigger decision #112 named for designing real limits, rather than guessing at claim shapes blind. `qualifiedModelId` first, `$.context.jwt.sub` second (not the reverse) because of a real, live-discovered ordering constraint — see decision #116. A single flat per-user budget, not a tiered scheme — this project has no user tiers (§2a/§2b's user model is one flat population), so there is no role/team claim to key a tiered limit on. This Gateway currently defaults to IAM auth (§2a decision #81's default), under which the caller is a fresh `$.context.jwt.sub` value only once/if the Gateway's authorizer is switched to JWT — until then, this rate limit's dimensioning is inert plumbing (matching decision #113's `GATEWAY_INFERENCE_URL` opt-in precedent of shipping additive, not-yet-fully-active infrastructure ahead of the feature that activates it), not a functioning per-user throttle yet. |
| 116 | Wildcard dimension values must be trailing, not leading — found via a real live `CreateGatewayRateLimit` rejection | The first live deploy attempt used `dimensionKeys=["$.context.jwt.sub", "qualifiedModelId"]` with `{sub: "*", qualifiedModelId: <concrete>}` — rejected outright with `"Wildcard '*' may only appear at trailing positions. Found non-wildcard value at position 1 after '*'"` (400 `InvalidRequest`), CloudFormation rolling back cleanly with zero partial state. Fixed by reversing the dimension order (decision #115's final shape: concrete value first, wildcard trailing). | A fourth distinct class of AWS-API constraint this one Gateway-routed-inference effort has now hit live (alongside the Gateway's own `authorizerType` immutability, the target-creation IAM-ordering issue, and the `provider`→`connector` target-replacement rejection — decisions #88/#108/#109) — this project's rate-limiting best-practices reading (this constraint is documented, just not applied correctly on the first attempt) reinforces the pattern established by decision #105 (the ADOT Fargate config): reading a constraint in a doc and correctly encoding it in code on the first attempt are two different things worth verifying separately, not assuming one implies the other. |
| 117 | Sustained model-call throttling (from decision #115's rate limit, or any other cause) is now caught and surfaced as a graceful chat message, not an uncaught exception | `agent/agent.py`'s `stream_agent_turn()` gained a second `except` clause, `ModelThrottledException`, alongside the pre-existing `MaxTokensReachedException` handler (decision #25) — yields `{"type": "error", "data": {"note": "I'm getting rate-limited right now — please try again in a minute or two."}}` rather than letting the exception propagate out of the AgentCore Runtime entrypoint uncaught. | Strands' own `ModelRetryStrategy` (default: 6 attempts, exponential backoff 4s→8s→16s→32s→64s) already retries a throttled model call transparently — confirmed by reading `strands/event_loop/_retry.py` directly rather than assuming — so this handler only fires once every retry is exhausted, i.e. genuinely sustained throttling, not a brief burst. Before this fix, that case had no handler at all: unlike `MaxTokensReachedException` (decision #25, added specifically because a real production 500 was observed), this gap was found and closed proactively, once decision #115 made throttling from this project's own configuration a real, expected possibility rather than only a hypothetical model-provider-side event. No `partial_text` key in the yielded event (unlike decision #25's handler) — throttling fails at the model-call layer before any tokens for that attempt are yielded, so there is nothing partial to preserve. |
| 118 | `aws-cdk-lib` bumped 2.265.0 → 2.268.0 specifically for `CfnGatewayRateLimit`, found via binary search rather than jumping straight to latest | Checked 2.266.0 and 2.267.0 directly (downloading each wheel and grepping the generated `aws_bedrockagentcore` module) before landing on 2.268.0 as the actual minimum version containing the construct — not assumed from "latest has it" alone. | A minimal-diff version bump is preferable to jumping to whatever the newest release happens to be, to limit the blast radius of unrelated upstream CDK changes landing in the same commit as this feature — confirmed via a full `cdk synth --all` and the full test suite (180/180) that the bump itself introduced no other breakage before adding the new resource on top of it. |
| 119 | `web/agent_client.py`'s streaming-request timeout changed from a single 60s float to a `(connect=5s, read=240s)` tuple, and the request now wraps the whole streaming-iteration loop in its `RuntimeError` contract, not just the initial `requests.post()` call | Found via live testing of decision #115's rate limit (deliberately set to a low, easily-triggered value temporarily — see this section's live-verification note below): a real, healthy turn that decision #117's `ModelThrottledException` handler correctly caught and converted to a graceful in-band SSE error event was instead killed client-side with a raw, uncaught `requests.exceptions.ReadTimeout`, surfacing to the browser as a generic "NetworkError" — never even reaching decision #117's friendly message. Root cause: a single-float `timeout` on a `stream=True` request in the `requests` library is a per-socket-read *inactivity* timeout (re-armed on every byte received), not a total-response deadline — confirmed against `requests`' own documented behavior, directly contradicting this module's own prior docstring comment, which had claimed the opposite ("streaming reads have no overall deadline once started"). Strands' `ModelRetryStrategy` (decision #117) yields nothing to the client during its up-to-124s of pure backoff sleep across 6 attempts, so that silence alone exceeded the old 60s timeout before the graceful frame could ever be delivered. Separately, the prior code only wrapped the initial `requests.post()` call in `try/except requests.RequestException` — a `ReadTimeout` raised later, mid-iteration inside `response.iter_lines()`, propagated as a raw, unwrapped exception, bypassing this function's own documented `RuntimeError` contract and, downstream, `web/server.py`'s `except RuntimeError` handling entirely. | 240s (read) comfortably clears Strands' worst-case silent gap with margin, while keeping the connect timeout tight (5s — establishing a connection should never legitimately take long, so no reason to relax that side). Wrapping the entire `requests.post()` + `iter_lines()` block in one `try/except requests.RequestException` (rather than only the initial call) ensures a mid-stream transport failure of any kind converts to the same `RuntimeError` contract a caller already handles, rather than needing a second, separate exception type threaded through every caller. Verified via two new regression tests (`test_raises_runtime_error_on_mid_stream_read_timeout`, `test_sends_connect_and_read_timeout_tuple`) and a real live reproduction: the exact "NetworkError" failure was reproduced once, then confirmed fixed on retry after this change — the browser correctly displayed decision #117's friendly rate-limited message instead. |
| 120 | Added `Limits(turns=AGENT_MAX_TURNS)` (default 30, env-overridable) to `agent.py`'s `stream_async()` call, with a graceful handler for the resulting `stop_reason == "limit_turns"` | Confirmed via reading Strands' own source (`strands/types/agent.py`, `strands/event_loop/event_loop.py`) that nothing previously bounded the number of agent-loop iterations (one model call plus any tool execution) for a plain `Agent` — `max_iterations` exists in Strands but only for the `Swarm` multiagent pattern, not a single `Agent`. Without this, a non-converging tool-call loop (e.g. the model repeatedly alternating `search`/`sequence` calls without making progress) had no purpose-built backstop: only the ~15-minute Runtime invocation timeout (coarse, wasteful) or decision #115's Gateway TPM budget (token-based, not iteration-based — a loop of cheap, low-token tool calls could iterate many times without ever tripping it) would eventually catch it. The chosen value of 30 was revised upward mid-implementation from an initial estimate of 15 after checking this codebase's own history: PLAN.md's post-Phase-7 `MaxTokensReachedException` fix documents a real, already-shipped production case — a 3-day, multi-stop itinerary that made 13 tool calls before writing its final answer — which 15 would have cut too close to with no real margin. | `Limits.turns` is a per-invocation keyword argument to `stream_async()`/`agent()`, not an `Agent()` constructor setting, and — unlike `MaxTokensReachedException`/`ModelThrottledException` — tripping it is not an exception: Strands ends the loop gracefully with `stop_reason: "limit_turns"`, checked explicitly in the `"result" in event` branch of `stream_agent_turn()` rather than via a new `except` block. 30 is sized to clear the documented 13-tool-call case with real margin under the conservative assumption that tool calls could be mostly sequential (Claude often batches several independent tool calls per turn, so the true turn count for that case was likely well under 13, but no live per-cycle breakdown confirms the exact ratio) — deliberately not a round number picked without checking, given this project's standing convention of grounding limits in real usage evidence rather than guesses. Deliberately did not also set `output_tokens`/`total_tokens` limits alongside `turns` — decision #115's Gateway-level TPM rate limit already covers the token/cost dimension, so a second, redundant token cap in `agent.py` would add complexity without a clear incremental benefit; `turns` alone directly targets the iteration-count failure mode this was scoped to close. |

deliberately, temporarily set to 500 TPM (from the real 50,000 default)
specifically to force real throttling on an ordinary test turn, deployed,
exercised live through the web UI, and reverted back to 50,000 immediately
after decision #119 above was confirmed fixed — this project's standing
convention of using real deploys to find real bugs rather than trusting
design-time reasoning alone (the exact convention decisions #88/#105/#108/
#109/#116 already established for this same Gateway-routed-inference
effort) applied a fifth time here, and caught a genuine, previously-latent
bug in `agent_client.py` that had existed since Phase 2's original
implementation, well before this rate-limiting work — it simply had no
reproduction path until a call could go silent for long enough to expose it.



1. User makes a vague request ("plan a trip to Kyoto").
2. Agent asks clarifying questions as needed: dates, trip length, budget,
   pace (relaxed/packed), interests, travelers/constraints. Uses short-term
   memory to avoid re-asking within a session; checks long-term memory for
   returning users' known preferences.
3. Once enough info is gathered, agent:
   - Calls Web Search for current info (events, closures, must-see spots).
   - Calls Location Service tool to geocode candidate places and sequence
     them into a sensible daily route (minimize backtracking).
   - Calls Weather tool (Open-Meteo) for the trip date range; adjusts
     outdoor/indoor activity mix if rain/extreme temps are forecast.
4. Agent renders a day-by-day itinerary as markdown chat text.
5. Long-term memory strategy extracts durable preferences from the
   conversation (e.g. "prefers walking tours," "traveling with kids") for
   future sessions.

## 5. Out of Scope (v1) — Explicit Fast-Follows

- Booking/payment integrations (flights, hotels, activities)
- Structured JSON itinerary output / export (PDF, calendar) — the web UI
  is a chat interface, not this fast-follow; it still only renders the
  same plain markdown output the agent produces.
- Integration/end-to-end automated tests
- Cost budgets/billing alarms

~~Cognito/JWT auth for non-AWS-credentialed clients~~ — attempted, see
§2a, then **reverted, see §2b decision #37.** IAM auth (decision #15) was
replaced by Okta-issued JWT bearer tokens (decisions #26–35), then
reverted back to IAM once `TravelAgentWebStack`'s OIDC-authenticated ALB
became the real human-facing identity boundary, making the Runtime's own
JWT authorizer redundant.

~~A hosted/multi-user frontend for the web UI~~ — **implemented, see §2b.**
`TravelAgentWebStack` hosts the web UI on ECS Fargate; it is now the only
supported client (§2d — the local CLI REPL that existed alongside the
local-only web UI, decision #19, has been removed entirely). Originally
fronted by an OIDC-authenticated ALB (§2b decision #38); as of §2e, the
ALB is a plain (non-authenticating) load balancer and the OIDC login flow
runs inside `web/server.py` itself.
